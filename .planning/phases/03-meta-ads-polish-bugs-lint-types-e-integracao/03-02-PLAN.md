---
phase: 03-meta-ads-polish-bugs-lint-types-e-integracao
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contexts/DashboardContext.tsx
  - src/contexts/AuthContext.tsx
  - src/App.tsx
  - src/components/auth/ProtectedRoute.tsx
  - src/components/dashboard/ToolApprovalDialog.tsx
  - src/hooks/use-chat-stream.ts
  - src/pages/settings/index.tsx
  - src/pages/dashboard/Recommendations.tsx
autonomous: true
requirements:
  - M2-03
  - M2-04

must_haves:
  truths:
    - "Switching platform Google -> Meta clears any stale Google selectedAccount usage in downstream queries (verified via code path inspection)"
    - "Switching platform Meta -> Google does NOT leave selectedMetaAccount as the active account for Google components"
    - "AuthContext.providerToken (Google) and any Meta connection state are independent — neither clears the other"
    - "DashboardIndex redirect in App.tsx correctly routes to /dashboard/overview when platform === 'google' and /dashboard/meta/overview when platform === 'meta'"
    - "use-chat-stream tool routing checks `platform === 'meta'` (or strictly typed AdPlatform) before invoking meta-mutate; switching platform mid-stream cancels or correctly scopes in-flight tool calls"
    - "ProtectedRoute does not gate Meta-only behavior with Google-specific state"
    - "Settings page exposes BOTH Google and Meta connection panels"
    - "localStorage keys do not collide between Google and Meta surfaces (Google uses adsinsight:selectedAccountId; Meta uses adsinsight:metaSelectedAccountId — both must remain distinct)"
    - "ToolApprovalDialog presents the correct tool name + platform context for Meta tools (no Google copy bleed-through)"
  artifacts:
    - path: ".planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-02-INTEGRATION-AUDIT.md"
      provides: "Documented findings for each of the 8 integration points + concrete fixes applied"
      contains: "## Audit Findings"
  key_links:
    - from: "src/App.tsx (DashboardIndex)"
      to: "useDashboard().platform"
      via: "Navigate to=meta/overview vs overview"
      pattern: "platform === 'meta'"
    - from: "src/hooks/use-chat-stream.ts"
      to: "meta-mutate / google-ads-mutate edge functions"
      via: "platform-conditional fetch URL"
      pattern: "meta-mutate|google-ads-mutate"
    - from: "src/contexts/DashboardContext.tsx"
      to: "localStorage"
      via: "STORAGE_SELECTED_ACCOUNT_ID vs STORAGE_META_ACCOUNT_ID"
      pattern: "adsinsight:(selectedAccountId|metaSelectedAccountId)"
---

<objective>
Audit the Google ↔ Meta integration boundary, document the behavior of each touchpoint, and fix any concrete bugs found. This plan is investigative — most of the work is reading, tracing, and documenting; fixes are surgical.

Purpose: Satisfy M2-03 ("free of dead integration paths between Google and Meta") and M2-04 ("no regression in Google Ads features") by proving every integration point behaves correctly during platform transitions.

Output: An audit document + any commits with concrete bug fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@CLAUDE.md

<interfaces>
<!-- Authoritative shapes the executor MUST respect during the audit -->

From src/contexts/DashboardContext.tsx (exact):
```typescript
export type AdPlatform = 'google' | 'meta';

const STORAGE_SELECTED_ACCOUNT_ID = 'adsinsight:selectedAccountId';     // GOOGLE
const STORAGE_META_ACCOUNT_ID    = 'adsinsight:metaSelectedAccountId';  // META — MUST NOT COLLIDE
const STORAGE_PLATFORM           = 'adsinsight:platform';
const STORAGE_HIDDEN_ACCOUNT_IDS = 'adsinsight:hiddenAccountIds';       // GOOGLE only
const STORAGE_TIMEZONE           = 'adsinsight:timezone';
const STORAGE_ATTRIBUTION_WINDOW = 'adsinsight:attributionWindow';

// Provided by useDashboard():
//   platform, setPlatform
//   selectedAccount (Google) / setSelectedAccount
//   selectedMetaAccount / setSelectedMetaAccount
//   accounts (Google) / metaAccounts
```

From src/App.tsx:
```tsx
function DashboardIndex() {
  const { platform } = useDashboard();
  return <Navigate to={platform === 'meta' ? 'meta/overview' : 'overview'} replace />;
}
```

From src/contexts/AuthContext.tsx:
- Google provider token lives in `providerToken` state + `sessionStorage['google_provider_token']`
- Meta connection is NOT in AuthContext — it lives in DB (`meta_accounts` table) + per-call tokens via meta-auth edge fn
- `signOut()` clears Google provider token but does NOT touch Meta state

From src/hooks/use-chat-stream.ts (current):
```typescript
interface UseChatStreamOptions {
  // ...
  platform?: 'google' | 'meta';  // defaults to 'google'
  metaAccessToken?: string | null;
  metaAccountId?: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and document the 8 integration points</name>
  <files>.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-02-INTEGRATION-AUDIT.md</files>
  <read_first>
    - src/contexts/DashboardContext.tsx (full)
    - src/contexts/AuthContext.tsx (full)
    - src/App.tsx (full)
    - src/components/auth/ProtectedRoute.tsx (full)
    - src/hooks/use-chat-stream.ts (full — pay attention to the platform branch + in-flight tool call refs)
    - src/components/dashboard/ToolApprovalDialog.tsx (full)
    - src/pages/settings/index.tsx (full)
    - src/pages/settings/AccountSection.tsx (full)
    - src/pages/dashboard/Recommendations.tsx (full)
    - src/components/layout/DashboardLayout.tsx (full — sidebar platform switch)
    - src/config/navigation.ts (full)
  </read_first>
  <action>
    Produce `.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-02-INTEGRATION-AUDIT.md` structured as:

    ```markdown
    # Phase 03 Plan 02 — Google ↔ Meta Integration Audit

    ## Audit Findings

    ### IP-1: DashboardContext platform switching
    - **File:** src/contexts/DashboardContext.tsx
    - **Question:** Does `setPlatform('meta')` leave Google `selectedAccount` referenced by Meta queries? Does `setPlatform('google')` leave Meta state leaking into Google views?
    - **Trace:** [walk through state — selectedAccount stays as-is; consumers must use platform-aware hooks]
    - **Verdict:** OK | BUG | RISK
    - **Fix:** [exact code change if BUG; "none" if OK]

    ### IP-2: AuthContext token isolation
    - **File:** src/contexts/AuthContext.tsx
    - **Question:** Does Google providerToken affect Meta operations? Does signing out of Google leave Meta connection intact (or vice versa)?
    - **Trace:** ...
    - **Verdict:** ...
    - **Fix:** ...

    ### IP-3: App.tsx DashboardIndex redirect
    - **File:** src/App.tsx
    - **Question:** Redirect correct in both directions? What if `platform` is unset on first load?
    - **Trace:** ...

    ### IP-4: use-chat-stream platform routing & in-flight tool calls
    - **File:** src/hooks/use-chat-stream.ts
    - **Question:** When user switches platform while a tool call is awaiting approval, does approval send the call to the right edge function? What if `platform` is the new value but `pendingToolCallRef` was captured under the old value?
    - **Trace:** Find every `platform === 'meta'` branch; map each to which edge function it invokes; check whether the `pendingToolCallRef` snapshot captures platform at capture-time or read-time.
    - **Verdict:** ...
    - **Fix:** if read-time (race condition), fix to capture platform inside `pendingToolCallRef`.

    ### IP-5: ProtectedRoute
    - **File:** src/components/auth/ProtectedRoute.tsx
    - **Question:** Does it guard Meta-only routes with only Google session? (Currently it checks `user` — does any Meta page require additional state?)
    - **Trace:** ...
    - **Verdict:** ...

    ### IP-6: Settings page — both panels visible/functional
    - **Files:** src/pages/settings/index.tsx, src/pages/settings/AccountSection.tsx
    - **Question:** Is the Meta connection panel rendered? Reachable? Does it function (button -> meta-auth)?
    - **Trace:** ...

    ### IP-7: Recommendations page platform awareness
    - **File:** src/pages/dashboard/Recommendations.tsx
    - **Question:** Is this Google-only? If so, is the entry hidden or disabled when `platform === 'meta'`? If shown, does it fetch Meta data correctly or crash?
    - **Trace:** ...

    ### IP-8: localStorage key collision
    - **Files:** src/contexts/DashboardContext.tsx, any consumer of localStorage with `adsinsight:` prefix
    - **Question:** Do Google and Meta keys collide? Are there any orphaned keys from removed features?
    - **Trace:** grep `localStorage` and `STORAGE_` constants. List every key.
    - **Verdict:** ...

    ### IP-9 (bonus): ToolApprovalDialog Google vs Meta copy
    - **File:** src/components/dashboard/ToolApprovalDialog.tsx
    - **Question:** Does the dialog show the right tool description per platform? Any Google-only copy showing up in a Meta tool approval?
    - **Trace:** Cross-reference TOOL_DESCRIPTION map in use-chat-stream.ts with dialog rendering.
    - **Verdict:** ...

    ## Summary
    - OK count: N
    - BUG count: N (fixed in Task 2)
    - RISK count: N (documented; out of scope to fix this phase)

    ## Bugs to Fix in Task 2
    [List with file + 1-line fix description]
    ```

    Use grep aggressively:
    - `grep -rn "platform === 'meta'" src/` to map all platform branches
    - `grep -rn "adsinsight:" src/` to enumerate localStorage keys
    - `grep -rn "providerToken" src/` to find Google token usage
    - `grep -rn "metaAccessToken\|meta_access_token" src/` to find Meta token usage
    - `grep -rn "google-ads-mutate\|meta-mutate" src/` to find mutation routing

    Do NOT modify any source file in this task — audit only.
  </action>
  <verify>
    <automated>type "C:\Users\Vanildo\Dev\ads-insights-ai\.planning\phases\03-meta-ads-polish-bugs-lint-types-e-integracao\03-02-INTEGRATION-AUDIT.md" | findstr /R "## IP- ## Summary ## Bugs"</automated>
  </verify>
  <acceptance_criteria>
    - File `03-02-INTEGRATION-AUDIT.md` exists in the phase directory
    - Document has sections IP-1 through IP-8 (plus IP-9 bonus), each with Verdict and Fix
    - Document has a `## Summary` section with counts
    - Document has a `## Bugs to Fix in Task 2` list
    - grep-verifiable: `grep -c "Verdict:" 03-02-INTEGRATION-AUDIT.md` returns at least 8
    - grep-verifiable: `grep -c "^### IP-" 03-02-INTEGRATION-AUDIT.md` returns at least 8
    - No source file was modified in this task (verify with `git diff --name-only` shows only the audit file)
  </acceptance_criteria>
  <done>Integration audit doc committed; bug list ready for Task 2 to fix.</done>
</task>

<task type="auto">
  <name>Task 2: Fix integration bugs found in audit</name>
  <files>
    src/contexts/DashboardContext.tsx,
    src/contexts/AuthContext.tsx,
    src/App.tsx,
    src/components/auth/ProtectedRoute.tsx,
    src/components/dashboard/ToolApprovalDialog.tsx,
    src/hooks/use-chat-stream.ts,
    src/pages/settings/index.tsx,
    src/pages/dashboard/Recommendations.tsx
  </files>
  <read_first>
    - .planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-02-INTEGRATION-AUDIT.md (the bug list from Task 1)
    - Each file before editing (read full)
  </read_first>
  <action>
    For each entry in the audit's `## Bugs to Fix in Task 2` list:
    1. Apply the documented fix exactly as scoped in the audit. No bonus refactors.
    2. Keep behavior changes minimal — every change must trace back to a documented bug.
    3. Preserve all existing logging patterns (`[Auth]`, `[DashboardContext]`, etc.) per CLAUDE.md convention.
    4. If a fix requires adding a regression test, prefer a small Vitest unit test in `src/**/__tests__/` colocated with the changed file. If the bug is purely structural (e.g., redirect path), code change alone is sufficient.

    Constraints:
    - DO NOT introduce any new feature, refactor unrelated code, or change Google-side behavior unless the audit explicitly identified a Google regression as the bug.
    - DO NOT change file-modification status for any file not in `files_modified` of this plan.
    - DO NOT delete the audit document; append a `## Resolution Log` section at the end with one bullet per bug listing (file, line approx, before -> after one-liner).

    After fixes:
    - Run `npm run lint` and ensure no NEW errors introduced (compare against 03-01 baseline if 03-01 already landed; if 03-02 runs in parallel with 03-01, just record total counts).
    - Run `npm run test` and ensure existing tests still pass.

    Coordination note (parallel-safe with 03-01):
    - This plan touches `src/hooks/use-chat-stream.ts` and `src/components/dashboard/ToolApprovalDialog.tsx`, which 03-01 may also touch.
    - If those two files conflict at merge time, prefer 03-01's lint-only edits first; reapply 03-02's behavioral fixes on top.
    - To avoid the conflict entirely, the executor should check `git status` before starting Task 2; if 03-01 is currently uncommitted on those two files, wait for 03-01 to commit before continuing.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | findstr /V /C:"process-attachment" /C:"tailwind.config.ts" | findstr /C:"problems"</automated>
  </verify>
  <acceptance_criteria>
    - Every bug listed in `## Bugs to Fix in Task 2` has a corresponding entry in `## Resolution Log` at the bottom of `03-02-INTEGRATION-AUDIT.md`
    - `npm run test` passes (no new failures introduced)
    - `npm run lint` does not show any error referencing the files modified by this task that did not already exist before
    - `tsc --noEmit -p tsconfig.app.json` exits 0
    - grep-verifiable: localStorage keys for Google and Meta remain distinct — `grep -E "adsinsight:(selectedAccountId|metaSelectedAccountId)" src/contexts/DashboardContext.tsx` returns BOTH constants
    - grep-verifiable: `grep -c "## Resolution Log" 03-02-INTEGRATION-AUDIT.md` returns at least 1
    - Google Ads regression check: no edits to any `src/pages/dashboard/{Overview,Campaigns,AdGroups,Ads,Keywords,SearchTerms,Audiences,Budgets,Conversions,Reports}.tsx` (Google pages). Verify with `git diff --name-only`.
  </acceptance_criteria>
  <done>All integration bugs identified in Task 1 are resolved; audit doc has resolution log; Google regression confirmed absent.</done>
</task>

</tasks>

<verification>
- 03-02-INTEGRATION-AUDIT.md exists, has 8+ IPs with verdicts, has Bugs list, has Resolution Log.
- All bugs in the Bugs list are addressed (Resolution Log matches).
- npm run test passes.
- npm run lint does not introduce new errors.
- tsc --noEmit -p tsconfig.app.json exits 0.
- No Google-only page was modified.
</verification>

<success_criteria>
- M2-03 partially satisfied (no dead integration paths between Google and Meta).
- M2-04 satisfied (no Google regression — Google pages untouched, integration changes audited).
- The audit document is the durable record of integration correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-02-SUMMARY.md` listing: integration points audited, verdicts, bugs fixed, regression-test additions (if any).
</output>
