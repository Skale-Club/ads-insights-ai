---
phase: 03-meta-ads-polish-bugs-lint-types-e-integracao
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - CLAUDE.md
  - .planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-03-BUILD-VERIFICATION.md
autonomous: true
requirements:
  - M2-03
  - M2-04
  - M2-05

must_haves:
  truths:
    - "`npm run build` completes successfully (exit 0) with the production build artifact in dist/"
    - "`npm run test` passes (all existing Vitest tests green; no new failures)"
    - "`tsc --noEmit -p tsconfig.node.json` passes (build config strict-mode validation)"
    - "No edge function or DB migration was deployed by Claude in this phase (M2-05)"
    - "CLAUDE.md reflects the expanded Meta surface (5 new pages, 13 new chat tools, 5 new meta-reports types) where descriptions of routes / edge fns / tables changed"
  artifacts:
    - path: ".planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-03-BUILD-VERIFICATION.md"
      provides: "Build + test + tsc command output captured for the record"
      contains: "## Build Verification"
    - path: "CLAUDE.md"
      provides: "Updated documentation reflecting Meta surface"
      contains: "Meta"
  key_links:
    - from: ".planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-03-BUILD-VERIFICATION.md"
      to: "npm run build, npm run test, tsc --noEmit -p tsconfig.node.json"
      via: "captured stdout/stderr per command"
      pattern: "## Build Verification"
---

<objective>
Final sweep: run the three quality gates (`npm run build`, `npm run test`, `tsc --noEmit -p tsconfig.node.json`), fix any failures uncovered, and refresh CLAUDE.md to reflect the expanded Meta surface from Phases 1-2. This plan runs in Wave 2 after Plans 03-01 (lint/type) and 03-02 (integration audit) have landed.

Purpose: Close out M2-03 (clean build + tests pass), M2-04 (no Google regression), M2-05 (no deploy by Claude — code only).
Output: Build verification document + CLAUDE.md refresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-CONTEXT.md
@.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-01-SUMMARY.md
@.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The three gates this plan runs -->

```bash
# Gate 1: production build (Vite)
npm run build
# Expected: exit 0, dist/ directory created

# Gate 2: tests (Vitest)
npm run test
# Expected: exit 0, all tests pass

# Gate 3: strict TS check for Vite config layer (strict mode ON in tsconfig.node.json)
npx tsc --noEmit -p tsconfig.node.json
# Expected: exit 0, no type errors
```

Constraint reminder (from M2-05):
- DO NOT run `supabase functions deploy ...`
- DO NOT run `supabase db push` or any migration command
- DO NOT push to any remote (no `git push`, no deploy hooks)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all three quality gates, capture output, fix failures</name>
  <files>.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-03-BUILD-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-01-SUMMARY.md (what 03-01 fixed)
    - .planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-02-SUMMARY.md (what 03-02 fixed)
    - tsconfig.node.json (note: strict ON here, OFF in tsconfig.app.json)
    - package.json (script definitions)
  </read_first>
  <action>
    Create `.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-03-BUILD-VERIFICATION.md` and populate it as follows:

    ```markdown
    # Phase 03 Plan 03 — Build Verification

    Run date: <ISO date>

    ## Build Verification

    ### Gate 1: npm run build
    Command: `npm run build`
    Exit code: <0 or N>
    Output (last 50 lines):
    ```
    <paste tail of output>
    ```
    Bundle size warnings: <yes/no — note any >500kb chunks, do NOT split here>
    Result: PASS | FAIL

    ### Gate 2: npm run test
    Command: `npm run test`
    Exit code: <0 or N>
    Output summary: <N passed, M failed>
    Result: PASS | FAIL

    ### Gate 3: tsc --noEmit -p tsconfig.node.json
    Command: `npx tsc --noEmit -p tsconfig.node.json`
    Exit code: <0 or N>
    Output: <paste, should be empty on PASS>
    Result: PASS | FAIL

    ## Failures and Fixes
    For each FAIL above:
    - Failure: <message>
    - Root cause: <one line>
    - Fix: <file + change> OR <"deferred — out of phase scope, logged below">

    ## Deferred / Out of Scope (do not fix here)
    - Pre-existing bundle size warning (>500kb main chunk) — split is a separate milestone per CONTEXT.md
    - Browserslist refresh (`npx update-browserslist-db@latest`) — optional, separate milestone
    - process-attachment/* lint debt — separate milestone
    - tailwind.config.ts lint debt — separate milestone

    ## M2-05 Deploy Boundary Check
    - No `supabase functions deploy` command run: CONFIRMED
    - No DB migration applied: CONFIRMED
    - No `git push` to any remote: CONFIRMED
    ```

    Execution flow:
    1. Run Gate 1 (`npm run build`). Tee output to a temp file. Capture exit code.
       - If FAIL: read the error, identify the file, fix it (only if root cause is in Phases 1-2 code — otherwise log as out-of-phase).
       - Re-run until PASS or it becomes clear the failure is out-of-scope (then document and stop).
    2. Run Gate 2 (`npm run test`). Same loop. Fix in-scope test failures; document pre-existing failures (none expected — STATE.md shows v1.1 complete).
    3. Run Gate 3 (`npx tsc --noEmit -p tsconfig.node.json`). Same loop. tsconfig.node.json has strict mode ON, so any new errors in vite.config.ts or similar must be fixed.
    4. Populate the verification document with final state.

    Bug-fix scope rules:
    - In-scope: any failure traceable to files introduced or modified in Phases 1-2 (Meta surface) or Phase 3 plans 01/02.
    - Out-of-scope: anything in process-attachment/, tailwind.config.ts, or pre-existing pre-v1.1 code paths.
    - If a Google file fails: it is a regression — STOP and fix immediately (M2-04).

    NEVER:
    - Run `supabase functions deploy` (M2-05)
    - Run any DB migration (M2-05)
    - Run `git push` (out of GSD scope unless user asks)
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test &amp;&amp; npx tsc --noEmit -p tsconfig.node.json</automated>
  </verify>
  <acceptance_criteria>
    - File `03-03-BUILD-VERIFICATION.md` exists with `## Build Verification`, `## Failures and Fixes`, `## Deferred / Out of Scope`, `## M2-05 Deploy Boundary Check` sections
    - All three gates report `Result: PASS` (or the document explains exactly why a remaining failure is pre-existing out-of-scope, with reference to deferred-items)
    - grep-verifiable: `grep -c "Result: PASS" 03-03-BUILD-VERIFICATION.md` returns 3
    - grep-verifiable: `grep "CONFIRMED" 03-03-BUILD-VERIFICATION.md` returns at least 3 lines (deploy boundary)
    - dist/ directory exists after build (Gate 1 actually ran and produced artifacts)
    - No Google dashboard page in `src/pages/dashboard/*.tsx` (non-meta) modified by this task — verify with `git diff --name-only`
  </acceptance_criteria>
  <done>All three gates green; deploy boundary respected; doc committed.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh CLAUDE.md with Meta surface additions</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full)
    - .planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-01-SUMMARY.md through 01-07-SUMMARY.md (Meta page surface from Phase 1)
    - .planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-01-PLAN.md through 02-05-PLAN.md (and any SUMMARYs that exist) — for chat tool surface
    - src/App.tsx (current route list)
    - src/config/navigation.ts (current nav)
    - supabase/functions/meta-reports/index.ts (current report types)
    - supabase/functions/meta-mutate/index.ts (current action list)
  </read_first>
  <action>
    Update CLAUDE.md surgically. Do NOT rewrite the file. Only add/update content where Phases 1-2 added surface area. Specifically:

    1. Under "Architecture > Frontend (src/)" -> "pages/" — note the `pages/dashboard/meta/` directory now contains 9 pages (Overview, Campaigns, AdSets, Ads, Audiences, Placements, Conversions, Reports, Budgets). Update wording if it understates current scope.

    2. Under "Supabase Edge Functions (supabase/functions/)" — add/update entries for:
       - `meta-reports/` — list the 11 report types currently supported (overview, campaigns, adsets, ads, insights_by_placement, daily_performance, audiences, placements, conversions, pixel-events, budgets-detail)
       - `meta-mutate/` — note it supports the full Meta lifecycle (create / update / pause / enable / batch / targeting / audience / lookalike / creative / schedule / A/B split)
       - `meta-auth/`, `meta-accounts/`, `get-meta-cli-session/`, `get-platform-config/`, `healthcheck/`, `google-ads-execute/`, `google-ads-mutate/`, `google-ads-reports/` — confirm these are listed (add if missing)

    3. Under "Database Tables (PostgreSQL via Supabase)" — add `meta_accounts` (per DashboardContext.tsx usage) if absent.

    4. Under "Auth Flow" — add a short paragraph noting Meta auth is a separate OAuth flow handled via `meta-auth` edge fn + `meta_accounts` table (not via Supabase Auth provider tokens like Google).

    5. Do NOT change:
       - The "Build & Development Commands" section (it is correct)
       - The "Path Alias" section
       - The "TypeScript" section (strict still off — accurate)
       - The "GSD Workflow Enforcement" section
       - The "Developer Profile" section (managed by another tool)
       - The "Project" -> "Ads Insights AI" tagline / Core Value (still accurate — both Google and Meta are now first-class)

    Keep edits minimal and factual. Match the existing tone and Markdown structure. No emojis unless already present.
  </action>
  <verify>
    <automated>findstr /C:"meta-reports" /C:"meta-mutate" /C:"meta_accounts" /C:"pages/dashboard/meta" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `meta-reports` and at least one of the 5 new report types (audiences | placements | conversions | pixel-events | budgets-detail)
    - CLAUDE.md contains `meta-mutate` referenced as a Meta lifecycle action handler
    - CLAUDE.md contains `meta_accounts` listed under database tables
    - CLAUDE.md mentions `pages/dashboard/meta/` (or equivalent) with awareness of the 9-page surface
    - No section headers were removed or reordered
    - grep-verifiable: `grep -c "meta-reports" CLAUDE.md` returns at least 1
    - grep-verifiable: `grep -c "meta_accounts" CLAUDE.md` returns at least 1
  </acceptance_criteria>
  <done>CLAUDE.md reflects current Meta surface accurately; future Claude sessions get correct context.</done>
</task>

</tasks>

<verification>
- 03-03-BUILD-VERIFICATION.md shows all three gates PASS.
- CLAUDE.md updated with Meta surface (greps confirm).
- No edge function deploy, no migration, no git push.
- dist/ exists from successful build.
- npm run test exits 0.
- npx tsc --noEmit -p tsconfig.node.json exits 0.
</verification>

<success_criteria>
- M2-03 fully satisfied (lint + type clean + integration audited + build + tests pass).
- M2-04 fully satisfied (no Google regression — confirmed by gates passing and Google pages untouched across Phase 3).
- M2-05 fully satisfied (no deploy or migration by Claude — explicit boundary check in verification doc).
- Phase 3 ready to ship.
</success_criteria>

<output>
After completion, create `.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-03-SUMMARY.md` listing: gates run, results, fixes applied, CLAUDE.md updates summary.
</output>
