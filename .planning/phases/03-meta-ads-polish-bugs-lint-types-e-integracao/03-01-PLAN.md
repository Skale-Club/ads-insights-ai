---
phase: 03-meta-ads-polish-bugs-lint-types-e-integracao
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/dashboard/meta/Audiences.tsx
  - src/pages/dashboard/meta/Placements.tsx
  - src/pages/dashboard/meta/Conversions.tsx
  - src/pages/dashboard/meta/Reports.tsx
  - src/pages/dashboard/meta/Budgets.tsx
  - src/pages/dashboard/meta/Overview.tsx
  - src/pages/dashboard/meta/Campaigns.tsx
  - src/pages/dashboard/meta/AdSets.tsx
  - src/pages/dashboard/meta/Ads.tsx
  - src/components/dashboard/ToolApprovalDialog.tsx
  - supabase/functions/meta-reports/index.ts
  - supabase/functions/meta-mutate/index.ts
  - supabase/functions/analyze-ads/index.ts
autonomous: true
requirements:
  - M2-03
  - M2-04

must_haves:
  truths:
    - "ESLint reports zero NEW errors in Meta-specific source files (pages/dashboard/meta/*, ToolApprovalDialog.tsx, meta-* edge fns, analyze-ads/index.ts Meta tool sections)"
    - "tsc --noEmit -p tsconfig.app.json passes (no new type errors in Meta code)"
    - "No usage of raw 'google' or 'meta' string literals outside the AdPlatform union type usage in any new Meta code"
    - "All Meta page React lists have explicit key props"
  artifacts:
    - path: ".planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-01-lint-baseline.txt"
      provides: "Captured ESLint baseline + delta (before/after fix counts)"
      contains: "BEFORE:"
  key_links:
    - from: "src/pages/dashboard/meta/*.tsx"
      to: "DashboardContext.tsx (AdPlatform type)"
      via: "import { AdPlatform } from '@/contexts/DashboardContext'"
      pattern: "AdPlatform"
---

<objective>
Fix all ESLint errors and TypeScript issues introduced by Phase 1 (5 Meta dashboard pages + meta-reports extensions) and Phase 2 (13 new chat tools across meta-mutate, analyze-ads, use-chat-stream additions, ToolApprovalDialog Meta-specific copy). Scope is strictly NEW code from Phases 1-2; do NOT touch pre-existing fluff (process-attachment/* `any` types, tailwind.config.ts, anything not introduced by Phases 1-2).

Purpose: Meet milestone requirement M2-03 (lint-clean, type-clean) and M2-04 (no Google regression) for the Meta surface.
Output: Cleaned source files + a baseline diff document proving zero NEW lint errors introduced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-CONTEXT.md
@CLAUDE.md
@eslint.config.js
@tsconfig.app.json

<interfaces>
<!-- Key types the executor must use for any platform-aware changes -->

From src/contexts/DashboardContext.tsx:
```typescript
export type AdPlatform = 'google' | 'meta';
export type DateRangePreset = 'last7' | 'last14' | 'last30' | 'thisMonth' | 'lastMonth' | 'thisYear' | 'lastYear' | 'allTime' | 'custom';
export type AttributionWindow = '7day' | '14day' | '30day' | '60day' | '90day';
export interface AdsAccount { id: string; customerId: string; name: string; currencyCode?: string; timeZone?: string; isManager?: boolean; status?: string; }
export interface MetaAdAccount { account_id: string; account_name: string; currency: string; account_status: number; is_selected: boolean; }
```

From use-chat-stream.ts (current platform-aware signature):
```typescript
interface UseChatStreamOptions {
  // ...
  platform?: 'google' | 'meta';
  metaAccessToken?: string | null;
  metaAccountId?: string | null;
}
```

ESLint flat config rules in effect (eslint.config.js):
- js.configs.recommended + ...tseslint.configs.recommended
- react-hooks/recommended
- react-refresh/only-export-components (warn)
- @typescript-eslint/no-unused-vars: OFF
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture lint baseline and triage NEW vs pre-existing</name>
  <files>.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-01-lint-baseline.txt</files>
  <read_first>
    - eslint.config.js (current ESLint config)
    - .planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-CONTEXT.md (fix policy: NEW code only)
    - .planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-01-SUMMARY.md through 01-07-SUMMARY.md (to know which files are NEW from Phase 1)
    - .planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-01-PLAN.md through 02-05-PLAN.md (to know which files Phase 2 touched)
  </read_first>
  <action>
    1. Run `npm run lint 2>&1 | tee .planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-01-lint-baseline.txt` to capture all ESLint output.
    2. At the top of the file, prepend a "BEFORE:" section with the total counts (errors / warnings).
    3. Build a NEW_FILES list (in-scope) at the top of the baseline file:
       - src/pages/dashboard/meta/Audiences.tsx
       - src/pages/dashboard/meta/Placements.tsx
       - src/pages/dashboard/meta/Conversions.tsx
       - src/pages/dashboard/meta/Reports.tsx
       - src/pages/dashboard/meta/Budgets.tsx
       - src/components/dashboard/ToolApprovalDialog.tsx (Meta-specific copy added in Phase 2)
       - supabase/functions/meta-reports/index.ts (5 new branches from 01-01)
       - supabase/functions/meta-mutate/index.ts (13 new handlers from 02-01)
       - supabase/functions/analyze-ads/index.ts (Meta tool definitions from 02-02)
       - src/hooks/use-chat-stream.ts (NEW Meta tool routing from 02-03; only NEW Meta branches in scope)
       - Any other file with Meta-specific additions referenced by Phase 1 or Phase 2 SUMMARYs
    4. Build an OUT_OF_SCOPE list (do not touch):
       - supabase/functions/process-attachment/* (pre-existing fluff)
       - tailwind.config.ts (pre-existing fluff)
       - Any error/warning not located inside a NEW_FILES file
    5. Mentally walk the lint output and tag each finding [IN_SCOPE] or [OUT_OF_SCOPE]. Append a triage summary to the baseline file:
       - Count of IN_SCOPE errors / warnings
       - Count of OUT_OF_SCOPE errors / warnings (these will not be touched by this plan)
    Do not fix anything yet. This task only captures the baseline + triage.
  </action>
  <verify>
    <automated>type "C:\Users\Vanildo\Dev\ads-insights-ai\.planning\phases\03-meta-ads-polish-bugs-lint-types-e-integracao\03-01-lint-baseline.txt"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-01-lint-baseline.txt` exists
    - File contains the string `BEFORE:` at the top
    - File contains a `NEW_FILES:` section listing the in-scope files
    - File contains a `OUT_OF_SCOPE:` section
    - File contains an `IN_SCOPE COUNT:` and `OUT_OF_SCOPE COUNT:` line
    - grep-verifiable: `grep -c "BEFORE:" 03-01-lint-baseline.txt` returns at least 1
  </acceptance_criteria>
  <done>Baseline file written with triage; team knows exactly which errors are in-scope.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix all IN_SCOPE lint + type errors in Meta code</name>
  <files>
    src/pages/dashboard/meta/Audiences.tsx,
    src/pages/dashboard/meta/Placements.tsx,
    src/pages/dashboard/meta/Conversions.tsx,
    src/pages/dashboard/meta/Reports.tsx,
    src/pages/dashboard/meta/Budgets.tsx,
    src/pages/dashboard/meta/Overview.tsx,
    src/pages/dashboard/meta/Campaigns.tsx,
    src/pages/dashboard/meta/AdSets.tsx,
    src/pages/dashboard/meta/Ads.tsx,
    src/components/dashboard/ToolApprovalDialog.tsx,
    supabase/functions/meta-reports/index.ts,
    supabase/functions/meta-mutate/index.ts,
    supabase/functions/analyze-ads/index.ts
  </files>
  <read_first>
    - .planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-01-lint-baseline.txt (the triage produced by Task 1)
    - Each in-scope file before editing
    - .planning/codebase/CONVENTIONS.md (project naming + style)
    - .planning/codebase/CONCERNS.md (known patterns to avoid)
  </read_first>
  <action>
    For each IN_SCOPE finding in the baseline file, fix the issue while obeying these rules:

    Fix policy (from 03-CONTEXT.md):
    - Fix: explicit `any` in Meta tool handler params, missing React keys, unused imports inside Meta files, missing `useEffect` deps, broken type imports, `prefer-const`, `no-case-declarations`, missing return types only where TypeScript complains.
    - Replace any raw string platform literal (e.g., `'google'` / `'meta'` used as a value type) with the `AdPlatform` union or a typed parameter. Import `AdPlatform` from `@/contexts/DashboardContext` where needed.
    - Prefer narrow Zod-inferred types in edge function handlers; if a handler accepted `(body: any)`, refactor to `(body: z.infer<typeof Schema>)` when a schema already exists.
    - For React keys in `.map()` returns, prefer a stable id field (e.g., `row.id`, `row.campaign_id`, `row.placementLabel`); only fall back to index if no stable id is available.
    - Replace `console.log(...)` calls that look like leftover debug noise in Meta page files (e.g., logs inside render bodies). Keep `[Tag]`-prefixed structured logs in hook/edge-function paths — they are the project convention per CLAUDE.md.

    DO NOT:
    - Touch any file outside the in-scope list
    - Refactor pre-existing helpers
    - Change behavior (no fix should alter rendered output or API call shape)
    - Re-introduce features deferred to other milestones (no strict mode, no Playwright, no perf passes)

    After all fixes, run `tsc --noEmit -p tsconfig.app.json` and ensure it passes for Meta files (it should already pass globally because strict is off; verify no new errors were introduced).

    Append an "AFTER:" section to `.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-01-lint-baseline.txt` with:
    - Re-run `npm run lint` output snippet
    - Counts: total errors / warnings (after)
    - IN_SCOPE delta: must be 0 errors / 0 warnings in Meta code
    - Note any IN_SCOPE finding that was intentionally left (with reason)
  </action>
  <verify>
    <automated>npm run lint 2>&1 | findstr /R /C:"error" /C:"problems" | findstr /V /C:"process-attachment" /C:"tailwind.config.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File `03-01-lint-baseline.txt` contains the string `AFTER:`
    - File `03-01-lint-baseline.txt` contains the string `IN_SCOPE delta:`
    - Re-running `npm run lint` shows zero errors in any of the listed `files_modified` paths (search lint output for those exact filenames; none should appear in the errors list)
    - `tsc --noEmit -p tsconfig.app.json` exits 0
    - grep-verifiable: `grep -c "AFTER:" 03-01-lint-baseline.txt` returns at least 1
    - grep-verifiable for AdPlatform usage: every Meta page that switches/branches on platform imports `AdPlatform` from `@/contexts/DashboardContext` (or uses the typed `platform` from `useDashboard()` directly). Pattern: `grep -l "platform === 'meta'" src/pages/dashboard/meta/*.tsx` returns files; each MUST also import `useDashboard`.
    - No file outside `files_modified` was modified in this task (verify with `git diff --name-only`)
  </acceptance_criteria>
  <done>
    Zero new lint errors in Meta surface. tsc passes. Triage delta documented.
  </done>
</task>

</tasks>

<verification>
- `npm run lint` output, filtered to exclude `process-attachment` and `tailwind.config.ts`, contains zero `error` lines that reference any of `files_modified`.
- `tsc --noEmit -p tsconfig.app.json` exits 0.
- The baseline file shows BEFORE/AFTER counts and an IN_SCOPE delta of 0.
- No git changes outside the declared `files_modified` plus the baseline file.
</verification>

<success_criteria>
- M2-03 partially satisfied for Meta surface (lint + type clean)
- M2-04 preserved (no Google file touched, so no Google regression possible from this plan)
- Baseline diff file lives in the phase directory and is committed
</success_criteria>

<output>
After completion, create `.planning/phases/03-meta-ads-polish-bugs-lint-types-e-integracao/03-01-SUMMARY.md` summarising: counts before/after, list of fixes per file, any intentionally-deferred items.
</output>
