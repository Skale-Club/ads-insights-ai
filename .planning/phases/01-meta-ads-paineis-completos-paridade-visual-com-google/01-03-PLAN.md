---
phase: 01-meta-ads-paineis-completos-paridade-visual-com-google
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/dashboard/meta/Placements.tsx
autonomous: true
requirements: [M2-01, M2-04]
must_haves:
  truths:
    - "User can navigate to /dashboard/meta/placements and see placement performance grouped by publisher_platform"
    - "Each group expands to show platform_position rows underneath"
    - "Loading, empty, error states all rendered"
    - "Page does not crash when there is no Meta account"
  artifacts:
    - path: "src/pages/dashboard/meta/Placements.tsx"
      provides: "Meta Placements page (publisher_platform grouping)"
      min_lines: 150
      contains: "export default function MetaPlacementsPage"
  key_links:
    - from: "src/pages/dashboard/meta/Placements.tsx"
      to: "useMetaReport('placements')"
      via: "named hook import"
      pattern: "useMetaReport.*placements"
---

<objective>
Build the Meta Placements page. No Google equivalent — follows shadcn card+table structure with grouping by publisher_platform (Facebook / Instagram / Messenger / Audience Network) per CONTEXT.md specifics.

Purpose: Placement insight is Meta-specific and one of the most actionable signals (which surfaces convert best).

Output: `src/pages/dashboard/meta/Placements.tsx` exporting `MetaPlacementsPage` as default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-CONTEXT.md
@src/pages/dashboard/meta/Campaigns.tsx
@src/pages/dashboard/meta/Overview.tsx

<interfaces>
Placements response shape (from edge function plan 01):
```ts
interface PlacementRow {
  id: string;
  publisherPlatform: string;     // 'facebook' | 'instagram' | 'messenger' | 'audience_network'
  platformPosition: string;      // 'feed' | 'stories' | 'reels' | 'right_hand_column' | ...
  impressionDevice: string;      // 'mobile_app' | 'desktop' | ...
  placementLabel: string;
  impressions: number;
  clicks: number;
  spend: number;
  ctr: number;
  cpc: number;
  reach: number;
  frequency: number;
  conversions: number;
  roas: number;
}
```
Hook returns `useQuery<PlacementRow[]>(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/pages/dashboard/meta/Placements.tsx with publisher_platform grouping</name>
  <files>src/pages/dashboard/meta/Placements.tsx</files>
  <read_first>
    - src/pages/dashboard/meta/Campaigns.tsx (Meta page styling reference — fmt(), Card, table)
    - src/pages/dashboard/meta/Overview.tsx (selectedMetaAccount guard pattern)
    - .planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-CONTEXT.md (specifics: group by publisher_platform first)
  </read_first>
  <action>
    Create `src/pages/dashboard/meta/Placements.tsx` with the following content:

    1. Imports: useMemo from react; Facebook, Instagram, MessageCircle, Globe, Loader2 from lucide-react; Card+CardContent+CardHeader+CardTitle+CardDescription from @/components/ui/card; Skeleton from @/components/ui/skeleton; Button from @/components/ui/button; Link from react-router-dom; useMetaReport from @/hooks/useMetaReport; useDashboard from @/contexts/DashboardContext.

    2. Define `interface PlacementRow` matching the shape in <interfaces> above.

    3. Define `fmt(n, type)` helper identical to the one in `src/pages/dashboard/meta/Overview.tsx` (currency / number / percent / roas).

    4. Define maps:
       - `platformIcon: Record<string, typeof Facebook> = { facebook: Facebook, instagram: Instagram, messenger: MessageCircle, audience_network: Globe }`
       - `platformLabel: Record<string, string> = { facebook: 'Facebook', instagram: 'Instagram', messenger: 'Messenger', audience_network: 'Audience Network' }`

    5. Define small `PlatformBadge({ platform })` helper component that picks icon + label from those maps (falls back to capitalized platform string).

    6. Export default function `MetaPlacementsPage()`:
       - Get `selectedMetaAccount` from `useDashboard()`
       - Call `useMetaReport<PlacementRow[]>('placements')` and destructure `{ data: rows, isLoading, error }`
       - `const placements = rows ?? []`
       - Build `grouped` via useMemo: Map by `publisherPlatform`, sort group items by spend desc, sort groups by totalSpend desc, return array of `{ platform, items, totalSpend, totalImpressions, totalConversions }`
       - Build `totals` via useMemo: `{ spend, impressions, clicks, conversions, placementCount }`
       - Early return: if no `selectedMetaAccount`, show "No Meta Ads account connected" prompt with Globe icon and `<Link to="/settings">Go to Settings</Link>` button
       - Early return: if `isLoading`, show centered `<Loader2 className="h-8 w-8 animate-spin text-primary" />` with "Loading placements..." text
       - Render:
         * `<h1>` "Placements" + selectedMetaAccount.account_name as subtitle
         * Error card (border-destructive/50 bg-destructive/5) when `error` truthy, displaying `(error as Error).message`
         * 4-column KPI grid: Total Spend, Impressions, Conversions, Placements (count)
         * If `grouped.length === 0`: empty state card "No placement data for this period"
         * Otherwise map `grouped.map((group) => <Card>...</Card>)`:
           - CardHeader has `<PlatformBadge platform={group.platform} />` left + CardDescription right showing `${fmt(group.totalSpend,'currency')} spent · ${fmt(group.totalConversions)} conv.`
           - CardContent has a `<table>` with columns: Position | Device | Impr. | Reach | Freq. | CTR | CPC | Spend | Conv. | ROAS
           - Row data: `r.platformPosition.replace(/_/g, ' ')` capitalized; `r.impressionDevice.replace(/_/g, ' ')` capitalized; numbers formatted via `fmt()`
           - ROAS shows `-` if 0
       - Wrap entire output in `<div className="space-y-6 animate-fade-in">`

    Do NOT register the route in App.tsx — Plan 07 does that. Do NOT touch any other file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/dashboard/meta/Placements.tsx','utf8');['export default function MetaPlacementsPage','useMetaReport','placements','publisherPlatform','platformPosition','impressionDevice','PlatformBadge','grouped','selectedMetaAccount','animate-fade-in'].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t)});console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/dashboard/meta/Placements.tsx` exists
    - Exports `MetaPlacementsPage` as default
    - Calls `useMetaReport<PlacementRow[]>('placements')`
    - Contains `publisherPlatform`, `platformPosition`, `impressionDevice` references
    - Has `grouped` useMemo computing per-platform groups
    - Has `selectedMetaAccount` guard
    - Has Loader2 loading state
    - Has error display branch using `(error as Error).message`
    - Has empty state `"No placement data for this period"`
    - Uses `animate-fade-in` wrapper class consistent with other Meta pages
    - File ≥ 150 lines
  </acceptance_criteria>
  <done>Meta Placements page file complete with grouped layout, loading/empty/error states. Ready for route wiring in plan 07.</done>
</task>

</tasks>

<verification>
- `npm run build:dev` succeeds
- grep checks listed above pass
- Google pages and other Meta pages unchanged
</verification>

<success_criteria>
- src/pages/dashboard/meta/Placements.tsx code-complete
- Page groups by publisher_platform (per CONTEXT.md specifics)
- Reuses existing fmt() helper pattern from Overview.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-03-SUMMARY.md` documenting the page structure and platform grouping logic.
</output>
