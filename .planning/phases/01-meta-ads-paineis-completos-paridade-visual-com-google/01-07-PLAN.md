---
phase: 01-meta-ads-paineis-completos-paridade-visual-com-google
plan: 07
type: execute
wave: 2
depends_on: [01-02, 01-03, 01-04, 01-05, 01-06]
files_modified:
  - src/App.tsx
  - src/config/navigation.ts
autonomous: true
requirements: [M2-01, M2-04]
must_haves:
  truths:
    - "Visiting /dashboard/meta/audiences renders MetaAudiencesPage"
    - "Visiting /dashboard/meta/placements renders MetaPlacementsPage"
    - "Visiting /dashboard/meta/conversions renders MetaConversionsPage"
    - "Visiting /dashboard/meta/reports renders MetaReportsPage"
    - "Visiting /dashboard/meta/budgets renders MetaBudgetsPage"
    - "metaNavItems exposes all 5 new pages"
    - "Existing Google routes still work (no regression)"
    - "DashboardIndex still routes meta users to meta/overview"
  artifacts:
    - path: "src/App.tsx"
      provides: "Route registrations for 5 new Meta pages"
      contains: "MetaAudiencesPage"
    - path: "src/config/navigation.ts"
      provides: "5 new entries in metaNavItems"
      contains: "Audiences"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/dashboard/meta/{Audiences,Placements,Conversions,Reports,Budgets}.tsx"
      via: "import + Route element prop"
      pattern: "path=\"meta/audiences\""
---

<objective>
Wire the 5 new Meta pages into the router and navigation config. This is the "make it reachable" step.

Purpose: Pages built in plans 02–06 don't reach users until routes and nav are registered.

Output: Updated `src/App.tsx` (5 new routes + 5 new imports) and `src/config/navigation.ts` (5 new entries in metaNavItems).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-CONTEXT.md
@src/App.tsx
@src/config/navigation.ts

<interfaces>
**Existing Meta route block in App.tsx** (lines ~99–102):
```tsx
{/* Meta Ads routes */}
<Route path="meta/overview" element={<MetaOverviewPage />} />
<Route path="meta/campaigns" element={<MetaCampaignsPage />} />
<Route path="meta/adsets" element={<MetaAdSetsPage />} />
<Route path="meta/ads" element={<MetaAdsPage />} />
```
Add 5 more `<Route>` lines beneath these. Add 5 corresponding `import` lines beside the existing Meta imports (around lines 37–40).

**Existing metaNavItems block in navigation.ts** (lines 24–31):
```ts
export const metaNavItems: NavItem[] = [
  { path: '/dashboard/meta/overview', label: 'Overview', icon: LayoutDashboard },
  { path: '/dashboard/meta/campaigns', label: 'Campaigns', icon: Megaphone },
  { path: '/dashboard/meta/adsets', label: 'Ad Sets', icon: Layers },
  { path: '/dashboard/meta/ads', label: 'Ads', icon: FileText },
  { path: '/dashboard/recommendations', label: 'AI Recommendations', icon: Lightbulb },
  { path: '/settings', label: 'Settings', icon: Settings },
];
```
Insert 5 new items BEFORE the "AI Recommendations" entry so the nav order matches Google's parity sequence.

Icons already imported at top of navigation.ts: `LayoutDashboard, Megaphone, KeyRound, Search, Lightbulb, Settings, FolderOpen, FileText, Users, DollarSign, BarChart3, Target, Layers`. Add `Globe` for Placements (not currently imported).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 5 new route imports and 5 new Route elements to src/App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (entire file)
    - src/pages/dashboard/meta/Audiences.tsx (must exist — built by plan 02)
    - src/pages/dashboard/meta/Placements.tsx (must exist — built by plan 03)
    - src/pages/dashboard/meta/Conversions.tsx (must exist — built by plan 04)
    - src/pages/dashboard/meta/Reports.tsx (must exist — built by plan 05)
    - src/pages/dashboard/meta/Budgets.tsx (must exist — built by plan 06)
  </read_first>
  <action>
    Modify `src/App.tsx`:

    1. **Add 5 import lines** immediately after the existing Meta page imports (currently lines 37–40 that import `MetaOverviewPage`, `MetaCampaignsPage`, `MetaAdSetsPage`, `MetaAdsPage`):
       ```tsx
       import MetaAudiencesPage from "@/pages/dashboard/meta/Audiences";
       import MetaPlacementsPage from "@/pages/dashboard/meta/Placements";
       import MetaConversionsPage from "@/pages/dashboard/meta/Conversions";
       import MetaReportsPage from "@/pages/dashboard/meta/Reports";
       import MetaBudgetsPage from "@/pages/dashboard/meta/Budgets";
       ```

    2. **Add 5 Route elements** immediately after the existing `<Route path="meta/ads" element={<MetaAdsPage />} />` (currently line 102) and before the closing `</Route>` of the dashboard parent route:
       ```tsx
       <Route path="meta/audiences" element={<MetaAudiencesPage />} />
       <Route path="meta/placements" element={<MetaPlacementsPage />} />
       <Route path="meta/conversions" element={<MetaConversionsPage />} />
       <Route path="meta/reports" element={<MetaReportsPage />} />
       <Route path="meta/budgets" element={<MetaBudgetsPage />} />
       ```

    Do NOT modify any other line. Do NOT change DashboardIndex, ProtectedRoute, or any provider wiring.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/App.tsx','utf8');['MetaAudiencesPage','MetaPlacementsPage','MetaConversionsPage','MetaReportsPage','MetaBudgetsPage','meta/audiences','meta/placements','meta/conversions','meta/reports','meta/budgets'].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t)});if(!s.includes('MetaOverviewPage'))throw new Error('regression: lost MetaOverviewPage');if(!s.includes('OverviewPage'))throw new Error('regression: lost Google OverviewPage');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `import MetaAudiencesPage from "@/pages/dashboard/meta/Audiences"`
    - grep finds `import MetaPlacementsPage from "@/pages/dashboard/meta/Placements"`
    - grep finds `import MetaConversionsPage from "@/pages/dashboard/meta/Conversions"`
    - grep finds `import MetaReportsPage from "@/pages/dashboard/meta/Reports"`
    - grep finds `import MetaBudgetsPage from "@/pages/dashboard/meta/Budgets"`
    - grep finds `<Route path="meta/audiences" element={<MetaAudiencesPage />} />`
    - grep finds `<Route path="meta/placements" element={<MetaPlacementsPage />} />`
    - grep finds `<Route path="meta/conversions" element={<MetaConversionsPage />} />`
    - grep finds `<Route path="meta/reports" element={<MetaReportsPage />} />`
    - grep finds `<Route path="meta/budgets" element={<MetaBudgetsPage />} />`
    - Existing imports (`MetaOverviewPage`, `OverviewPage`, `AudiencesPage` etc.) still present (no regression)
  </acceptance_criteria>
  <done>5 new Meta routes registered alongside existing 4. No Google route removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 5 new entries to metaNavItems in src/config/navigation.ts</name>
  <files>src/config/navigation.ts</files>
  <read_first>
    - src/config/navigation.ts (entire file — small)
  </read_first>
  <action>
    Modify `src/config/navigation.ts`:

    1. **Add `Globe` to the lucide-react import** on line 1:
       Change `import { LayoutDashboard, Megaphone, KeyRound, Search, Lightbulb, Settings, FolderOpen, FileText, Users, DollarSign, BarChart3, Target, Layers, LucideIcon } from 'lucide-react';`
       to: `import { LayoutDashboard, Megaphone, KeyRound, Search, Lightbulb, Settings, FolderOpen, FileText, Users, DollarSign, BarChart3, Target, Layers, Globe, LucideIcon } from 'lucide-react';`

    2. **Insert 5 new NavItem entries into `metaNavItems`** BEFORE the existing `{ path: '/dashboard/recommendations', label: 'AI Recommendations', icon: Lightbulb }` line. The final `metaNavItems` array should look exactly like:
       ```ts
       export const metaNavItems: NavItem[] = [
         { path: '/dashboard/meta/overview', label: 'Overview', icon: LayoutDashboard },
         { path: '/dashboard/meta/campaigns', label: 'Campaigns', icon: Megaphone },
         { path: '/dashboard/meta/adsets', label: 'Ad Sets', icon: Layers },
         { path: '/dashboard/meta/ads', label: 'Ads', icon: FileText },
         { path: '/dashboard/meta/audiences', label: 'Audiences', icon: Users },
         { path: '/dashboard/meta/placements', label: 'Placements', icon: Globe },
         { path: '/dashboard/meta/conversions', label: 'Conversions', icon: Target },
         { path: '/dashboard/meta/reports', label: 'Reports', icon: BarChart3 },
         { path: '/dashboard/meta/budgets', label: 'Budgets', icon: DollarSign },
         { path: '/dashboard/recommendations', label: 'AI Recommendations', icon: Lightbulb },
         { path: '/settings', label: 'Settings', icon: Settings },
       ];
       ```

    Do NOT touch the Google `navItems` array.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/config/navigation.ts','utf8');['/dashboard/meta/audiences','/dashboard/meta/placements','/dashboard/meta/conversions','/dashboard/meta/reports','/dashboard/meta/budgets','Globe'].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t)});if(!s.includes('/dashboard/overview'))throw new Error('regression: Google nav lost');if(!s.includes('/dashboard/meta/overview'))throw new Error('regression: Meta overview nav lost');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `Globe` in lucide-react import statement
    - grep finds all 5 strings: `/dashboard/meta/audiences`, `/dashboard/meta/placements`, `/dashboard/meta/conversions`, `/dashboard/meta/reports`, `/dashboard/meta/budgets`
    - grep finds labels `Audiences`, `Placements`, `Conversions`, `Reports`, `Budgets` in the file
    - `metaNavItems` array length is now 11 entries (was 6)
    - Google `navItems` array unchanged — `/dashboard/overview`, `/dashboard/campaigns` etc still present
  </acceptance_criteria>
  <done>Sidebar shows all 9 Meta pages when platform === 'meta', matching the Google sidebar count + parity intent.</done>
</task>

</tasks>

<verification>
- `npm run build:dev` succeeds — verifies all 5 imports resolve to real files
- Manual smoke (plan 08): visit each new /dashboard/meta/* path, observe page renders
- Google routes untouched
</verification>

<success_criteria>
- 5 new routes registered in App.tsx
- 5 new nav entries in metaNavItems
- No regression on Google side (verified by grep)
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-07-SUMMARY.md` listing the new routes and nav entries.
</output>
