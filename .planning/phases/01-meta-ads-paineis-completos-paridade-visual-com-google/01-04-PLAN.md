---
phase: 01-meta-ads-paineis-completos-paridade-visual-com-google
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/dashboard/meta/Conversions.tsx
autonomous: true
requirements: [M2-01, M2-04]
must_haves:
  truths:
    - "User can navigate to /dashboard/meta/conversions and see a funnel (impressions → clicks → add-to-cart → purchases)"
    - "User can see per-campaign conversion table with purchases, leads, costPerPurchase, ROAS"
    - "User can see per-action-type pixel events list (purchase, lead, view_content, etc.)"
    - "Loading, empty, and error states render correctly"
  artifacts:
    - path: "src/pages/dashboard/meta/Conversions.tsx"
      provides: "Meta Conversions page mirroring Google's funnel-style metric presentation"
      min_lines: 200
      contains: "export default function MetaConversionsPage"
  key_links:
    - from: "src/pages/dashboard/meta/Conversions.tsx"
      to: "useMetaReport('conversions') and useMetaReport('pixel-events')"
      via: "two parallel hook calls"
      pattern: "useMetaReport.*conversions"
---

<objective>
Build the Meta Conversions page. Mirrors Google's funnel-style metric presentation per CONTEXT.md specifics: total conversions → cost per conversion → conversion rate → conversion value.

Purpose: One of the most important pages for Meta marketers — answers "is my pixel firing and what's it tracking".

Output: `src/pages/dashboard/meta/Conversions.tsx` exporting `MetaConversionsPage` as default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-CONTEXT.md
@src/pages/dashboard/Conversions.tsx
@src/pages/dashboard/meta/Overview.tsx
@src/pages/dashboard/meta/Campaigns.tsx

<interfaces>
Two report types feed this page:

**`conversions` response shape** (from edge function plan 01):
```ts
interface ConversionsResponse {
  perCampaign: Array<{
    id: string;
    campaignId: string;
    campaignName: string;
    spend: number;
    impressions: number;
    clicks: number;
    purchases: number;
    leads: number;
    addToCart: number;
    purchaseValue: number;
    costPerPurchase: number;
    roas: number;
  }>;
  totals: {
    spend: number;
    impressions: number;
    clicks: number;
    addToCart: number;
    purchases: number;
    leads: number;
    purchaseValue: number;
    costPerPurchase: number;
    conversionRate: number;  // percent
    roas: number;
  };
}
```

**`pixel-events` response shape** (from edge function plan 01):
```ts
interface PixelEvent {
  id: string;
  actionType: string;   // 'offsite_conversion.fb_pixel_purchase' etc.
  count: number;
  value: number;
  costPerAction: number;
  roas: number;
}
// Hook returns PixelEvent[]
```

Pretty labels for common action types:
- `offsite_conversion.fb_pixel_purchase` → "Purchase"
- `offsite_conversion.fb_pixel_lead` → "Lead"
- `offsite_conversion.fb_pixel_add_to_cart` → "Add to Cart"
- `offsite_conversion.fb_pixel_view_content` → "View Content"
- `offsite_conversion.fb_pixel_initiate_checkout` → "Initiate Checkout"
- `offsite_conversion.fb_pixel_complete_registration` → "Complete Registration"
- `page_engagement` → "Page Engagement"
- `link_click` → "Link Click"
- `landing_page_view` → "Landing Page View"
- fallback: strip `offsite_conversion.fb_pixel_` prefix and replace `_` with space
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/pages/dashboard/meta/Conversions.tsx with funnel + per-campaign table + pixel events</name>
  <files>src/pages/dashboard/meta/Conversions.tsx</files>
  <read_first>
    - src/pages/dashboard/Conversions.tsx (Google parity reference — KPI grid, funnel layout, table styling)
    - src/pages/dashboard/meta/Campaigns.tsx (Meta page conventions — fmt, sort, table)
    - src/pages/dashboard/meta/Overview.tsx (selectedMetaAccount guard, fmt helper)
    - .planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-CONTEXT.md
  </read_first>
  <action>
    Create `src/pages/dashboard/meta/Conversions.tsx` with:

    1. **Imports**: useMemo from react; Target, ShoppingCart, UserPlus, MousePointer, Eye, Zap, Loader2, AlertCircle from lucide-react; Card+CardContent+CardHeader+CardTitle+CardDescription from @/components/ui/card; Skeleton from @/components/ui/skeleton; Progress from @/components/ui/progress; Button from @/components/ui/button; Link from react-router-dom; useMetaReport from @/hooks/useMetaReport; useDashboard from @/contexts/DashboardContext; cn from @/lib/utils.

    2. **Type definitions**: ConversionsResponse and PixelEvent interfaces exactly as in <interfaces>.

    3. **Helper**: `fmt(n, type)` identical to Overview.tsx.

    4. **Helper**: `prettyActionType(s: string): string` mapping the exact strings from <interfaces> "Pretty labels" section above. Use a literal `Record<string, string>` for the mapped ones; fallback strips `offsite_conversion.fb_pixel_` prefix and replaces underscores with spaces with title-case.

    5. **Default export**: `MetaConversionsPage()`:
       - Pull `selectedMetaAccount` from `useDashboard()`
       - Call `const { data: conv, isLoading: cLoading, error: cError } = useMetaReport<ConversionsResponse>('conversions');`
       - Call `const { data: events, isLoading: eLoading, error: eError } = useMetaReport<PixelEvent[]>('pixel-events');`
       - Compute `const isLoading = cLoading || eLoading;`
       - Early return: no `selectedMetaAccount` → connect prompt with Target icon + Link to /settings
       - Early return: `isLoading` → Loader2 spinner "Loading conversions..."
       - Render `<h1>Conversions</h1>` + selectedMetaAccount.account_name subtitle
       - Render error card (border-destructive/50) showing `cError` or `eError` message if either present
       - **Funnel section** (Card): horizontal funnel showing 5 stages with Progress bars (% relative to impressions):
         * Impressions (100%)
         * Clicks (`(totals.clicks/impressions)*100`%)
         * Add to Cart (`(totals.addToCart/impressions)*100`%)
         * Purchases (`(totals.purchases/impressions)*100`%)
         * Leads (`(totals.leads/impressions)*100`%)
         Each stage shows label + count + percentage. Use `<Progress value={pct} className="h-2" />`.
       - **KPI grid** (4 cards): Total Purchases (with `conversionRate.toFixed(2)%` rate), Cost per Purchase (currency), Purchase Value (currency), ROAS (with `'x'` suffix, color-coded: text-success if >=2, text-destructive if <1)
       - **Per-campaign table** (Card titled "Conversions by Campaign"):
         * Columns: Campaign | Spend | Impr. | Clicks | Add to Cart | Purchases | Leads | Cost/Purchase | ROAS
         * Sort `conv.perCampaign` by `purchases` desc by default
         * Empty state: "No campaign conversion data"
       - **Pixel events list** (Card titled "Pixel Events", description: "Events received from your Meta Pixel and conversion API"):
         * Render `events.map(e => ...)` showing icon (use Target by default; ShoppingCart for purchase/add_to_cart; UserPlus for lead/registration; Eye for view_content/landing_page_view; MousePointer for link_click)
         * Each row: Icon + prettyActionType(e.actionType) + count badge + value (currency if >0) + costPerAction (currency if >0)
         * Empty state: "No pixel events captured. Verify your Meta Pixel is installed."
       - Wrap in `<div className="space-y-6 animate-fade-in">`

    Do NOT register the route in App.tsx — Plan 07 does that. Do NOT touch other files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/dashboard/meta/Conversions.tsx','utf8');['export default function MetaConversionsPage','useMetaReport','conversions','pixel-events','perCampaign','totals','prettyActionType','funnel','selectedMetaAccount','animate-fade-in','Progress'].forEach(t=>{if(!s.toLowerCase().includes(t.toLowerCase()))throw new Error('missing '+t)});console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/dashboard/meta/Conversions.tsx` exists
    - Exports `MetaConversionsPage` as default
    - Calls `useMetaReport<ConversionsResponse>('conversions')` AND `useMetaReport<PixelEvent[]>('pixel-events')`
    - Contains `prettyActionType` helper function
    - Contains a funnel section using `<Progress>` for at least 4 stages (impressions, clicks, add-to-cart, purchases)
    - Contains a per-campaign table with at minimum: Campaign, Spend, Purchases, Cost/Purchase, ROAS columns
    - Contains a pixel-events section
    - Has `selectedMetaAccount` guard with Link to /settings
    - Has Loader2 loading state combining both queries
    - Has error display branch handling both `cError` and `eError`
    - Has empty states for both per-campaign and pixel-events sections
    - File ≥ 200 lines
  </acceptance_criteria>
  <done>Meta Conversions page complete with funnel, per-campaign table, and pixel events. Ready for route wiring in plan 07.</done>
</task>

</tasks>

<verification>
- `npm run build:dev` succeeds
- grep checks above pass
- Google Conversions page unchanged
</verification>

<success_criteria>
- src/pages/dashboard/meta/Conversions.tsx code-complete
- Funnel UI present (per CONTEXT.md specifics)
- Both `conversions` and `pixel-events` report types consumed
</success_criteria>

<output>
After completion, create `.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-04-SUMMARY.md` documenting structure, both data sources, and funnel logic.
</output>
