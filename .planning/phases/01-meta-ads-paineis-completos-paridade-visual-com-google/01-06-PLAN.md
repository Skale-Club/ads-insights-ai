---
phase: 01-meta-ads-paineis-completos-paridade-visual-com-google
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/dashboard/meta/Budgets.tsx
autonomous: true
requirements: [M2-01, M2-04]
must_haves:
  truths:
    - "User can navigate to /dashboard/meta/budgets and see campaign + ad-set budgets with utilization"
    - "Each row shows daily vs lifetime distinction"
    - "Each row shows a pacing bar (% of budget spent)"
    - "Over-budget rows are visually flagged"
  artifacts:
    - path: "src/pages/dashboard/meta/Budgets.tsx"
      provides: "Meta Budgets page with pacing bars and over-budget alerts"
      min_lines: 180
      contains: "export default function MetaBudgetsPage"
  key_links:
    - from: "src/pages/dashboard/meta/Budgets.tsx"
      to: "useMetaReport('budgets-detail')"
      via: "named hook import"
      pattern: "useMetaReport.*budgets-detail"
---

<objective>
Build the Meta Budgets page mirroring Google's Budgets page with Meta-specific additions: daily vs lifetime distinction + pacing bar per CONTEXT.md specifics.

Purpose: Marketers need to see budget pacing across campaigns and ad sets to catch over/underspend.

Output: `src/pages/dashboard/meta/Budgets.tsx` exporting `MetaBudgetsPage` as default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-CONTEXT.md
@src/pages/dashboard/Budgets.tsx
@src/pages/dashboard/meta/Campaigns.tsx
@src/pages/dashboard/meta/Overview.tsx

<interfaces>
**`budgets-detail` response shape** (from edge function plan 01):
```ts
interface BudgetRow {
  id: string;
  level: 'campaign' | 'adset';
  name: string;
  campaignId: string;
  status: string;                          // 'ACTIVE' | 'PAUSED' | ...
  budgetType: 'daily' | 'lifetime' | 'none';
  amount: number;                          // dollars (already divided by 100)
  spent: number;                           // dollars
  remaining: number;                       // dollars
  utilization: number;                     // percent (e.g. 87.5)
  bidStrategy: string | null;
}

interface BudgetsDetailResponse {
  campaigns: BudgetRow[];
  adsets: BudgetRow[];
}
// useMetaReport<BudgetsDetailResponse>('budgets-detail')
```

**Status to badge variant map** (from Meta Campaigns.tsx):
```ts
const statusVariant = { ACTIVE: 'default', PAUSED: 'secondary', ARCHIVED: 'outline', DELETED: 'destructive' };
```

**shadcn components**:
- `Tabs, TabsList, TabsTrigger, TabsContent` from '@/components/ui/tabs'
- `Progress` from '@/components/ui/progress'
- `Badge` from '@/components/ui/badge'
- `Card, ...` from '@/components/ui/card'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/pages/dashboard/meta/Budgets.tsx with campaign + adset tabs and pacing bars</name>
  <files>src/pages/dashboard/meta/Budgets.tsx</files>
  <read_first>
    - src/pages/dashboard/Budgets.tsx (Google parity reference — KPI grid, over-budget alert card, utilization Progress bar pattern)
    - src/pages/dashboard/meta/Campaigns.tsx (statusVariant map, Badge usage, fmt helper)
    - src/pages/dashboard/meta/Overview.tsx (selectedMetaAccount guard, fmt)
    - .planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-CONTEXT.md (specifics: pacing bar)
  </read_first>
  <action>
    Create `src/pages/dashboard/meta/Budgets.tsx` with:

    1. **Imports**: useMemo from react; DollarSign, AlertTriangle, Layers, Megaphone, Loader2 from lucide-react; Card+CardContent+CardHeader+CardTitle+CardDescription from '@/components/ui/card'; Tabs+TabsList+TabsTrigger+TabsContent from '@/components/ui/tabs'; Badge from '@/components/ui/badge'; Progress from '@/components/ui/progress'; Button from '@/components/ui/button'; Link from 'react-router-dom'; useMetaReport from '@/hooks/useMetaReport'; useDashboard from '@/contexts/DashboardContext'; cn from '@/lib/utils'.

    2. **Types**: `BudgetRow` and `BudgetsDetailResponse` exactly as in <interfaces>.

    3. **Helpers**:
       - `fmt(n, type)` identical to Overview.tsx
       - `const statusVariant: Record<string, 'default' | 'secondary' | 'destructive' | 'outline'> = { ACTIVE: 'default', PAUSED: 'secondary', ARCHIVED: 'outline', DELETED: 'destructive' };`

    4. **BudgetTable component** `function BudgetTable({ rows }: { rows: BudgetRow[] })`:
       - Empty state: "No budgets in this view"
       - Render table with columns: Name | Status | Type | Budget | Spent | Remaining | Utilization | Bid Strategy
       - For Utilization column render `<Progress value={Math.min(r.utilization, 100)} className="h-2" />` plus the number formatted as `r.utilization.toFixed(0)%`, color-coded: text-destructive if >100, text-warning if 80-100, text-success otherwise.
       - For Type column show capitalized r.budgetType.
       - For Status column show `<Badge variant={statusVariant[r.status] ?? 'outline'}>{r.status}</Badge>`
       - Remaining shown in red (text-destructive) if negative

    5. **Default export** `MetaBudgetsPage()`:
       - `const { selectedMetaAccount } = useDashboard();`
       - `const { data, isLoading, error } = useMetaReport<BudgetsDetailResponse>('budgets-detail');`
       - `const campaigns = data?.campaigns ?? [];`
       - `const adsets = data?.adsets ?? [];`
       - `const all = [...campaigns, ...adsets];`
       - Compute stats useMemo: `{ totalBudget, totalSpent, totalRemaining, overBudget: all.filter(b => b.utilization > 100).length, campaignsCount: campaigns.length, adsetsCount: adsets.length }`
       - Early return: no selectedMetaAccount → connect prompt with DollarSign icon + Link to /settings
       - Early return: isLoading → Loader2 spinner "Loading budgets..."
       - Render:
         * `<h1>Budgets</h1>` + account_name subtitle
         * Error card if `error`
         * Over-budget alert card if `stats.overBudget > 0` (border-destructive/50 bg-destructive/5, AlertTriangle icon, text "X budget(s) exceeded the planned amount")
         * 4-column KPI grid: Total Budget, Total Spent, Remaining (red if negative), Active Budgets (count)
         * Tabs with defaultValue="campaigns":
           - TabsTrigger "campaigns" (Megaphone icon) "Campaigns ({campaignsCount})"
           - TabsTrigger "adsets" (Layers icon) "Ad Sets ({adsetsCount})"
           - TabsContent "campaigns": Card "Campaign Budgets" with `<BudgetTable rows={campaigns} />`
           - TabsContent "adsets": Card "Ad Set Budgets" with `<BudgetTable rows={adsets} />`
       - Wrap in `<div className="space-y-6 animate-fade-in">`

    Do NOT register the route in App.tsx — Plan 07 does that.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/dashboard/meta/Budgets.tsx','utf8');['export default function MetaBudgetsPage','useMetaReport','budgets-detail','BudgetTable','Progress','overBudget','utilization','statusVariant','selectedMetaAccount','Tabs','adsets','animate-fade-in'].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t)});console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/dashboard/meta/Budgets.tsx` exists
    - Exports `MetaBudgetsPage` as default
    - Calls `useMetaReport<BudgetsDetailResponse>('budgets-detail')`
    - Has `BudgetTable` inner component
    - Uses `<Progress>` for utilization pacing bar (per CONTEXT.md specifics)
    - Has `statusVariant` map matching Meta Campaigns convention
    - Has over-budget alert when any row has `utilization > 100`
    - Has Tabs with `campaigns` and `adsets` values
    - Distinguishes daily vs lifetime via budgetType field
    - Has `selectedMetaAccount` guard with Link to /settings
    - Has loading state via Loader2
    - File ≥ 180 lines
  </acceptance_criteria>
  <done>Meta Budgets page complete with campaign+adset tabs, pacing bars, over-budget alerts.</done>
</task>

</tasks>

<verification>
- `npm run build:dev` succeeds
- grep checks above pass
- Google Budgets page unchanged
</verification>

<success_criteria>
- src/pages/dashboard/meta/Budgets.tsx code-complete
- Pacing bar per row (per CONTEXT.md specifics)
- Daily vs lifetime distinction visible
</success_criteria>

<output>
After completion, create `.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-06-SUMMARY.md` documenting structure and pacing logic.
</output>
