---
phase: 01-meta-ads-paineis-completos-paridade-visual-com-google
plan: 08
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05, 01-06, 01-07]
files_modified: []
autonomous: false
requirements: [M2-01, M2-04, M2-05]
must_haves:
  truths:
    - "npm run build:dev exits with code 0 (no TypeScript/build errors)"
    - "npm run lint exits with code 0 for the 7 newly-created/modified files (warnings allowed)"
    - "User has visually confirmed each of the 5 new Meta pages renders in the browser"
    - "User has visually confirmed Google dashboard still works (no regression)"
    - "supabase functions deploy was NOT invoked (M2-05 honored)"
  artifacts: []
  key_links: []
---

<objective>
Final verification of phase 1. Two-stage check:
1. Automated: build + lint pass on the files touched in this phase
2. Human visual smoke: confirm each new page renders and does not crash

Purpose: Catch type errors and obvious wiring mistakes before declaring the phase shipped. Honor M2-05 (no edge function deploy).

Output: No new files. Verification result documented in summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run build and lint, report any errors</name>
  <files>package.json (read-only)</files>
  <read_first>
    - package.json (confirm build:dev and lint scripts present)
  </read_first>
  <action>
    Run these commands sequentially from the repo root:

    1. `npm run build:dev` — capture output. If exit code != 0, STOP and report TypeScript/build errors. Do NOT proceed to lint or smoke.

    2. `npm run lint -- --max-warnings 9999 src/pages/dashboard/meta/Audiences.tsx src/pages/dashboard/meta/Placements.tsx src/pages/dashboard/meta/Conversions.tsx src/pages/dashboard/meta/Reports.tsx src/pages/dashboard/meta/Budgets.tsx src/App.tsx src/config/navigation.ts src/hooks/useMetaReport.ts` — capture output. Report errors (not warnings). If lint errors found, list them so the user can decide whether to fix now or defer to Phase 3 polish.

    3. Confirm by listing `supabase/functions/meta-reports/` and verifying no `.deployed` artifact or recent deploy log — the only change should be the file edit from plan 01.

    Report results in the task output.
  </action>
  <verify>
    <automated>npm run build:dev 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:dev` exit code is 0
    - `npm run lint` exit code is 0 for the listed files (warnings allowed via --max-warnings 9999, errors fail)
    - No `supabase functions deploy` command was invoked (grep shell history is not required, but Claude must not have run that command in any prior plan)
  </acceptance_criteria>
  <done>Build clean. Lint clean (errors-only). Edge function still un-deployed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual smoke test of all 5 new pages</name>
  <files>(no file changes — user verifies in browser)</files>
  <action>See <how-to-verify> below: human-only verification step. Pause and wait for user signal before declaring phase shipped.</action>
  <verify><automated>echo "awaiting human verification"</automated></verify>
  <done>User has typed "approved" or described issues for follow-up.</done>
  <what-built>
    5 new dashboard pages under /dashboard/meta/:
    - audiences (4-tab breakdown)
    - placements (publisher-grouped table)
    - conversions (funnel + per-campaign + pixel events)
    - reports (field selector + xlsx export)
    - budgets (campaign+adset tabs with pacing bars)

    Plus router + nav wiring.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:8080
    2. Sign in and switch to Meta platform (whatever the existing platform switcher mechanism is)
    3. In the sidebar, confirm 5 new menu items appear: Audiences, Placements, Conversions, Reports, Budgets
    4. Click each in turn:
       - **Audiences**: confirm 4 tabs (Age &amp; Gender, Region, Device, Publisher), default tab is Age &amp; Gender, table or "No data" appears in each tab
       - **Placements**: confirm grouped cards (one per publisher_platform), each with a table inside; OR an empty state if your test account has no placement data
       - **Conversions**: confirm funnel section (5 stages with Progress bars), KPI grid, per-campaign table, pixel events list
       - **Reports**: confirm field selector (checkboxes), click "Export as XLSX", confirm a file downloads with `.xlsx` extension and contains your campaign rows
       - **Budgets**: confirm Campaigns + Ad Sets tabs, each row has a Progress utilization bar, over-budget rows (if any) show red. Top alert card appears only if any row > 100%.
    5. Switch back to Google platform — confirm Google sidebar shows the same items as before, no Meta items leak in, all Google pages still load
    6. Confirm no console errors (open DevTools console, look for red entries while clicking each page)

    If any page crashes (white screen / React error), capture the error message + which page.
    If any data shape mismatch shows up (NaN, undefined, "Cannot read properties"), capture it.

    NOTE: The edge function changes from plan 01 are NOT deployed. If the new report types return errors from Meta's API or the dispatcher, that is expected until the user deploys. For verification purposes, "page renders + empty state" is acceptable; "page crashes on render" is not.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 pages render without crashing and Google side is unaffected.
    Otherwise describe what broke: "Page X crashed with error Y", and a follow-up gap-closure plan will be created.
  </resume-signal>
</task>

</tasks>

<verification>
- Build: `npm run build:dev` exit 0
- Lint: `npm run lint` exit 0 (or only warnings)
- Visual: user confirms all 5 pages reachable and not crashing
- No deploy: edge function file changed but `supabase functions deploy meta-reports` not run
</verification>

<success_criteria>
- All 5 new pages reachable from sidebar when on Meta platform
- All 5 pages render without runtime crash (empty data states allowed)
- Google dashboard unaffected
- Build + lint clean for new/modified files
- Edge function NOT deployed (M2-05 honored)
- User has explicit visual confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-08-SUMMARY.md` recording:
- Build/lint results
- Pages verified by user
- Any issues encountered (for Phase 3 polish backlog)
- Reminder for user: "Deploy meta-reports edge function when ready: `supabase functions deploy meta-reports`"
</output>
