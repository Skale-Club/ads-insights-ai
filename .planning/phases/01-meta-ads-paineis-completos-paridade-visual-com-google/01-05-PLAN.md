---
phase: 01-meta-ads-paineis-completos-paridade-visual-com-google
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/dashboard/meta/Reports.tsx
autonomous: true
requirements: [M2-01, M2-04]
must_haves:
  truths:
    - "User can navigate to /dashboard/meta/reports and pick which fields to include in an exported report"
    - "User can click an Export button that downloads an xlsx file using the existing xlsx library"
    - "Generated file contains rows from current Meta campaigns + selected fields"
    - "Empty state shown when no campaign data"
  artifacts:
    - path: "src/pages/dashboard/meta/Reports.tsx"
      provides: "Meta custom report builder with xlsx export"
      min_lines: 180
      contains: "export default function MetaReportsPage"
  key_links:
    - from: "src/pages/dashboard/meta/Reports.tsx"
      to: "xlsx library"
      via: "import * as XLSX from 'xlsx' + XLSX.writeFile"
      pattern: "from ['\"]xlsx['\"]"
---

<objective>
Build the Meta Reports page — a custom report builder with field selector and xlsx export using the existing `xlsx` library (already in deps).

Purpose: Meta marketers need to export their data for client reporting. Mirrors Google's Reports page intent.

Output: `src/pages/dashboard/meta/Reports.tsx` exporting `MetaReportsPage` as default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-CONTEXT.md
@src/pages/dashboard/Reports.tsx
@src/pages/dashboard/meta/Campaigns.tsx
@package.json

<interfaces>
**Reusing existing `campaigns` report type** (no new edge function needed for this page):
```ts
interface MetaCampaign {
  id: string;
  name: string;
  status: string;
  objective: string;
  budgetType: string;
  budget: number;
  spend: number;
  impressions: number;
  clicks: number;
  ctr: number;
  roas: number;
  results: number;
}
// useMetaReport<MetaCampaign[]>('campaigns')
```

**xlsx library** (already in package.json):
```ts
import * as XLSX from 'xlsx';
const ws = XLSX.utils.json_to_sheet(rows);
const wb = XLSX.utils.book_new();
XLSX.utils.book_append_sheet(wb, ws, 'Campaigns');
XLSX.writeFile(wb, 'meta-report.xlsx');
```

**Date range** from `useDashboard()`:
```ts
const { dateRange } = useDashboard();
// dateRange.from, dateRange.to are Date objects
```

**Available shadcn components**:
- `import { Checkbox } from '@/components/ui/checkbox'`
- `import { Label } from '@/components/ui/label'`
- `import { Button } from '@/components/ui/button'`
- `import { Card, ... } from '@/components/ui/card'`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/pages/dashboard/meta/Reports.tsx with field selector and xlsx export</name>
  <files>src/pages/dashboard/meta/Reports.tsx</files>
  <read_first>
    - src/pages/dashboard/Reports.tsx (Google parity reference — KPI cards, export button)
    - src/pages/dashboard/meta/Campaigns.tsx (campaign type, fmt helper, Meta page conventions)
    - src/pages/dashboard/meta/Overview.tsx (selectedMetaAccount guard)
    - package.json (confirm xlsx is present)
  </read_first>
  <action>
    Create `src/pages/dashboard/meta/Reports.tsx` with:

    1. **Imports**:
       - `useState, useMemo` from react
       - `Download, FileSpreadsheet, BarChart3, Loader2, Calendar` from lucide-react
       - `* as XLSX from 'xlsx'`
       - `format` from 'date-fns'
       - `Card, CardContent, CardHeader, CardTitle, CardDescription` from '@/components/ui/card'
       - `Checkbox` from '@/components/ui/checkbox'
       - `Label` from '@/components/ui/label'
       - `Button` from '@/components/ui/button'
       - `Skeleton` from '@/components/ui/skeleton'
       - `Link` from 'react-router-dom'
       - `useMetaReport` from '@/hooks/useMetaReport'
       - `useDashboard` from '@/contexts/DashboardContext'
       - `useToast` from '@/hooks/use-toast'

    2. **Type**: `interface MetaCampaign` matching <interfaces> shape.

    3. **Field definitions constant**:
       ```ts
       const AVAILABLE_FIELDS: Array<{ key: keyof MetaCampaign; label: string; default: boolean }> = [
         { key: 'name', label: 'Campaign Name', default: true },
         { key: 'status', label: 'Status', default: true },
         { key: 'objective', label: 'Objective', default: true },
         { key: 'budgetType', label: 'Budget Type', default: false },
         { key: 'budget', label: 'Budget', default: true },
         { key: 'spend', label: 'Spend', default: true },
         { key: 'impressions', label: 'Impressions', default: true },
         { key: 'clicks', label: 'Clicks', default: true },
         { key: 'ctr', label: 'CTR (%)', default: true },
         { key: 'roas', label: 'ROAS', default: true },
         { key: 'results', label: 'Results', default: true },
       ];
       ```

    4. **Export function** `exportToXlsx(rows, selectedFields, accountName, dateRange)`:
       ```ts
       const mapped = rows.map(r => {
         const obj: Record<string, any> = {};
         selectedFields.forEach(f => {
           const def = AVAILABLE_FIELDS.find(x => x.key === f);
           if (def) obj[def.label] = r[f];
         });
         return obj;
       });
       const ws = XLSX.utils.json_to_sheet(mapped);
       const wb = XLSX.utils.book_new();
       XLSX.utils.book_append_sheet(wb, ws, 'Campaigns');
       const from = format(dateRange.from, 'yyyy-MM-dd');
       const to = format(dateRange.to, 'yyyy-MM-dd');
       const filename = `meta-${accountName.replace(/[^a-z0-9]+/gi, '-').toLowerCase()}-${from}-to-${to}.xlsx`;
       XLSX.writeFile(wb, filename);
       return filename;
       ```

    5. **Default export** `MetaReportsPage()`:
       - `const { selectedMetaAccount, dateRange } = useDashboard();`
       - `const { data: campaigns, isLoading, error } = useMetaReport<MetaCampaign[]>('campaigns');`
       - `const { toast } = useToast();`
       - `const [selected, setSelected] = useState<Set<keyof MetaCampaign>>(new Set(AVAILABLE_FIELDS.filter(f => f.default).map(f => f.key)));`
       - Toggle handler: `const toggle = (k: keyof MetaCampaign) => setSelected(prev => { const next = new Set(prev); if (next.has(k)) next.delete(k); else next.add(k); return next; });`
       - Stats useMemo: `{ campaignsCount: campaigns?.length ?? 0, totalSpend, selectedFieldsCount: selected.size }`
       - Handler `handleExport()`:
         ```ts
         if (!campaigns || campaigns.length === 0) {
           toast({ title: 'No data', description: 'Nothing to export for this period.' });
           return;
         }
         try {
           const filename = exportToXlsx(campaigns, Array.from(selected), selectedMetaAccount?.account_name ?? 'meta', dateRange);
           toast({ title: 'Report exported', description: filename });
         } catch (err: any) {
           toast({ title: 'Export failed', description: err.message, variant: 'destructive' });
         }
         ```
       - Early return: no selectedMetaAccount → connect prompt with FileSpreadsheet icon
       - Early return: isLoading → Loader2 spinner "Loading report builder..."
       - Render:
         * `<h1>Reports</h1>` + subtitle showing account_name and date range "from – to"
         * Error card if `error`
         * 3-column KPI grid: Campaigns Count, Total Spend, Selected Fields
         * Main two-column layout (`grid gap-6 lg:grid-cols-3`):
           - Left col (`lg:col-span-2`): Card titled "Field Selection" with description "Pick the columns to include in the export". CardContent grid of checkboxes (2 columns on md+) — each item: `<Checkbox id={field.key} checked={selected.has(field.key)} onCheckedChange={() => toggle(field.key)} /> <Label htmlFor={field.key}>{field.label}</Label>`
           - Right col: Card titled "Export" with description, big `<Button onClick={handleExport} disabled={selected.size === 0 || isLoading || !campaigns?.length}><Download className="h-4 w-4 mr-2" />Export as XLSX</Button>`. Show count "Will include {campaigns.length} rows × {selected.size} columns" below button.
       - Wrap in `<div className="space-y-6 animate-fade-in">`

    Do NOT register the route in App.tsx — Plan 07 does that.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/dashboard/meta/Reports.tsx','utf8');['export default function MetaReportsPage','useMetaReport','campaigns','from \\'xlsx\\'','XLSX.writeFile','json_to_sheet','book_new','book_append_sheet','AVAILABLE_FIELDS','handleExport','selectedMetaAccount','Checkbox'].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t)});console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/dashboard/meta/Reports.tsx` exists
    - Exports `MetaReportsPage` as default
    - Imports `xlsx` via `import * as XLSX from 'xlsx'`
    - Calls `XLSX.writeFile`, `XLSX.utils.json_to_sheet`, `XLSX.utils.book_new`, `XLSX.utils.book_append_sheet`
    - Has `AVAILABLE_FIELDS` constant with at least 9 field entries
    - Uses `<Checkbox>` for field selection toggles
    - Has `handleExport` function wired to a `<Button>` with `Download` icon
    - Has `selectedMetaAccount` guard
    - Has loading state via Loader2
    - Filename pattern `meta-{slug}-{from}-to-{to}.xlsx`
    - File ≥ 180 lines
  </acceptance_criteria>
  <done>Meta Reports page complete with field selector and working xlsx export. Reuses existing `campaigns` report type (no new edge function call needed).</done>
</task>

</tasks>

<verification>
- `npm run build:dev` succeeds
- grep checks above pass
- xlsx library import present (already in package.json)
</verification>

<success_criteria>
- src/pages/dashboard/meta/Reports.tsx code-complete with xlsx export
- Reuses existing `useMetaReport('campaigns')` — no new edge function call
- Field selector with sensible defaults
</success_criteria>

<output>
After completion, create `.planning/phases/01-meta-ads-paineis-completos-paridade-visual-com-google/01-05-SUMMARY.md` documenting field set, export filename pattern, and rationale for reusing campaigns reportType.
</output>
