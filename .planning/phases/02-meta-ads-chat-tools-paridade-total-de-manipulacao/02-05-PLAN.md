---
phase: 02-meta-ads-chat-tools-paridade-total-de-manipulacao
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - .planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-VERIFICATION.md
autonomous: true
requirements: [M2-02, M2-04, M2-05]
must_haves:
  truths:
    - "VERIFICATION.md exists and lists all 13 new tools with status (callable / requires-App-Review / requires-user-smoke-test)"
    - "Lint passes on all 4 modified files (no new errors introduced)"
    - "TypeScript compiles on the 3 frontend files (npx tsc --noEmit) with no new errors"
    - "Edge function syntax check via `deno check` passes on meta-mutate/index.ts and analyze-ads/index.ts"
    - "No edge function deploy invoked (M2-05)"
  artifacts:
    - path: ".planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-VERIFICATION.md"
      provides: "Documented verification of Phase 02 + Meta App Review requirements user must complete before live usage"
      contains: "Meta App Review, ads_management_standard_access, custom_audiences, deploy"
  key_links:
    - from: "VERIFICATION.md"
      to: "user runbook"
      via: "Pre-launch checklist section"
      pattern: "Pre-launch Checklist"
---

<objective>
Run static checks on the 4 files modified in Plans 02-01 through 02-04, then produce `02-VERIFICATION.md` documenting (a) which of the 13 new tools are immediately usable, (b) which require Meta App Review before live usage, and (c) the pre-launch checklist the user must complete before deploying the edge functions.

Purpose: Phase 02 is code-only per M2-05 — verification = static checks + documentation, NOT live API calls (which would require deployed functions and Meta App Review approval).
Output: Single VERIFICATION.md file with checklist; no code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-CONTEXT.md
@.planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-01-PLAN.md
@.planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-02-PLAN.md
@.planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-03-PLAN.md
@.planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-04-PLAN.md

<interfaces>
<!-- Files modified across the phase -->
- supabase/functions/meta-mutate/index.ts (Plan 02-01)
- supabase/functions/analyze-ads/index.ts (Plan 02-02)
- supabase/functions/analyze-ads/system-prompt.ts (Plan 02-02)
- src/hooks/use-chat-stream.ts (Plan 02-03)
- src/components/dashboard/ToolApprovalDialog.tsx (Plan 02-04)

<!-- Static check commands available in repo -->
- npm run lint
- npm run build
- npx tsc --noEmit -p tsconfig.app.json
- deno check supabase/functions/<name>/index.ts (if deno installed; OPTIONAL fallback)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run static checks (lint + tsc) and capture results</name>
  <files>(none — read-only verification step)</files>
  <read_first>
    - package.json (confirm `lint` and `build` scripts present)
    - All 5 files modified in prior plans (to confirm they exist on disk)
  </read_first>
  <action>
    Run these commands sequentially and capture stdout+stderr to memory for use in Task 2. Do NOT abort on individual failures — collect everything, then evaluate.

    1. **TypeScript check on app code** — must pass with exit 0 (no new errors introduced):
       ```
       npx tsc --noEmit -p tsconfig.app.json
       ```
       Note: the project has strict mode OFF per CLAUDE.md, so the bar is "no NEW errors", not "zero errors". If tsc reports errors, diff against the pre-Phase-02 baseline mentally — only flag errors located in the 3 frontend files modified (src/hooks/use-chat-stream.ts, src/components/dashboard/ToolApprovalDialog.tsx).

    2. **ESLint on the 3 modified frontend files** (scoped — don't lint the whole repo, that's Phase 03's job):
       ```
       npx eslint src/hooks/use-chat-stream.ts src/components/dashboard/ToolApprovalDialog.tsx
       ```
       Expected: 0 errors. Warnings acceptable for this phase (Phase 03 handles polish).

    3. **Deno check on edge functions** (best-effort — skip silently if `deno` is not installed; Supabase will type-check on deploy):
       ```
       deno check supabase/functions/meta-mutate/index.ts
       deno check supabase/functions/analyze-ads/index.ts
       ```
       If `deno` is not available on PATH, fall back to a syntax sanity check: `node -e "require('fs').readFileSync('supabase/functions/meta-mutate/index.ts','utf8').length"` (just confirms file is readable + non-empty).

    4. **Grep sanity check — confirm all 13 new tool names round-trip across the 4 files**:
       ```
       for tool in createCampaign createAdSet createAd duplicateCampaign duplicateAdSet updateTargeting updateBidStrategy updateCreative updateSchedule createCustomAudience createLookalikeAudience batchPauseEnable createSplitTest; do
         echo "=== $tool ==="
         grep -c "$tool" supabase/functions/meta-mutate/index.ts supabase/functions/analyze-ads/index.ts supabase/functions/analyze-ads/system-prompt.ts src/hooks/use-chat-stream.ts src/components/dashboard/ToolApprovalDialog.tsx
       done
       ```
       Expect each tool name to appear in ALL 5 files (meta-mutate handler + analyze-ads tool def + system prompt mention + use-chat-stream dispatch + ToolApprovalDialog description).

    5. **Confirm NO deploy command was run** — grep last 200 git diff lines for `supabase functions deploy` (should return 0 matches):
       ```
       git log --since="1 day ago" --all --oneline | head -20
       ```
       (Pure sanity — Claude is not supposed to deploy per M2-05.)
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
    - `npx eslint src/hooks/use-chat-stream.ts src/components/dashboard/ToolApprovalDialog.tsx` exits 0
    - All 13 new tool names appear with count >= 1 in each of the 5 modified files
    - No `supabase functions deploy` invocation in recent git history (M2-05)
  </acceptance_criteria>
  <done>Static checks pass on all modified files; all 13 tool names round-trip across the 4 layers (edge handler + tool def + prompt + frontend dispatch + modal copy).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 02-VERIFICATION.md documenting tool status, App Review requirements, and pre-launch checklist</name>
  <files>.planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-VERIFICATION.md</files>
  <read_first>
    - Results from Task 1 (in working memory — tsc exit code, eslint exit code, tool-name grep counts)
    - .planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-CONTEXT.md (Permission-Gated Features section — App Review scopes)
  </read_first>
  <action>
    Create `.planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-VERIFICATION.md` with the EXACT structure below. Fill in static-check status from Task 1 results. If tsc/eslint reported issues, document them in the "Known Issues" section instead of marking pass — do not skip mentioning failures.

    ```markdown
    # Phase 02 Verification — Meta Ads Chat Tools Parity

    **Phase:** 02-meta-ads-chat-tools-paridade-total-de-manipulacao
    **Verified:** {today's date YYYY-MM-DD}
    **Mode:** Static + documentation (no deploy per M2-05)

    ## Scope Recap

    Expanded Meta chat tool surface from 5 → 18 tools, adding full lifecycle manipulation (create / edit / duplicate / batch / target / audience / creative / schedule / A/B) with the existing approval flow.

    ## Static Checks

    | Check | Command | Result |
    |-------|---------|--------|
    | TypeScript | `npx tsc --noEmit -p tsconfig.app.json` | {PASS / FAIL — paste actual result} |
    | ESLint (modified frontend files) | `npx eslint src/hooks/use-chat-stream.ts src/components/dashboard/ToolApprovalDialog.tsx` | {PASS / FAIL} |
    | Deno check (edge functions, best-effort) | `deno check supabase/functions/meta-mutate/index.ts supabase/functions/analyze-ads/index.ts` | {PASS / FAIL / SKIPPED if deno unavailable} |
    | Tool-name round-trip | grep all 13 names across 5 files | {PASS — all 13 present in all 5 layers / FAIL — list missing} |

    ## Tool Status

    Each new tool is exposed end-to-end (tool def → system prompt → frontend dispatch → modal copy → edge handler). Live usage depends on the user's Meta access token scopes and (for 2 tools) App Review status.

    | Tool | Code Status | Live Status Notes |
    |------|-------------|-------------------|
    | createCampaign | Callable | Needs `ads_management` scope (standard for Marketing API) |
    | createAdSet | Callable | Same |
    | createAd | Callable | Same; user must supply a valid creative_id or object_story_spec with a Page they admin |
    | duplicateCampaign | Callable | Uses `/copies` endpoint; new campaign starts paused |
    | duplicateAdSet | Callable | Same |
    | updateTargeting | Callable | Targeting payload must be valid Meta spec; partial update preserved |
    | updateBidStrategy | Callable | COST_CAP/BID_CAP/LOWEST_COST_WITH_BID_CAP require bidAmountCents |
    | updateCreative | Callable | Inline creative path creates a new adcreative first, then attaches |
    | updateSchedule | Callable | start_time/end_time/adset_schedule snake_case per Meta API |
    | createCustomAudience | **GATED** — requires Meta App Review (`ads_management_standard_access` + `custom_audiences` scope) | Returns friendly App Review error if scope missing (code 200, subcode 1487) |
    | createLookalikeAudience | **GATED** — same as above | Same |
    | batchPauseEnable | Callable | Hard cap 50 per call (enforced by Zod); partial failures returned in `results[]` |
    | createSplitTest | Callable | Uses `/ad_studies` endpoint; cells must conform to Meta /ad_studies spec |

    ## Approval Flow

    All 13 new tools route through the existing `ToolApprovalDialog` before invoking `meta-mutate`. Read-only tools (`queryMetaData`, `analyzeCreative`) bypass — same as before. Risk levels per Plan 02-03:

    - **High risk** (creates + audience + batch): createCampaign, createAdSet, createAd, createCustomAudience, createLookalikeAudience, batchPauseEnable
    - **Medium risk** (duplicates + targeted updates): duplicateCampaign, duplicateAdSet, updateTargeting, updateBidStrategy, updateCreative, updateSchedule, createSplitTest

    The two App Review-gated tools show an in-modal yellow banner explaining the scope requirement.

    ## Pre-launch Checklist (for the user)

    Before this phase goes live, the user must:

    - [ ] **Deploy edge functions** (Claude is NOT permitted to deploy per M2-05):
      ```
      npx supabase functions deploy meta-mutate
      npx supabase functions deploy analyze-ads
      ```
    - [ ] **Confirm Meta access token scopes** on the connected Meta account include at minimum `ads_management` and `pages_show_list`. For audiences, additionally `ads_management_standard_access` + `custom_audiences` (App Review-gated).
    - [ ] **Submit Meta App Review** for the two audience tools if you intend to use them in production:
        - `ads_management_standard_access` permission tier
        - `custom_audiences` permission
        - Provide a screencast showing how the chat triggers `createCustomAudience` with user approval
    - [ ] **Smoke test in dev** with a low-budget test campaign:
        1. Open chat on a Meta account
        2. Ask: "Create a paused test campaign called 'phase-02-smoke' with objective traffic and $5/day"
        3. Verify ApprovalModal appears with `createCampaign` + the campaign details
        4. Approve → confirm a paused campaign with that name appears in Ads Manager
        5. Repeat for `duplicateCampaign`, `updateBudget`, `batchPauseEnable`
    - [ ] **Roll back plan**: keep the previous edge function version handy via `npx supabase functions list` — if the new tools cause issues, redeploy the prior version.

    ## Known Issues

    {List any lint warnings, tsc errors, or grep mismatches caught in Task 1. If none, write "None."}

    ## Deferred (out of scope per CONTEXT.md)

    - Bulk CSV import
    - Campaign performance forecasting before creation
    - Creative auto-generation
    - Auto-bid adjustments via scheduled job
    - Cross-account campaign cloning

    ## Files Modified

    - `supabase/functions/meta-mutate/index.ts` — +13 action handlers + 2 helpers (wrapMetaError, resolveAccountId)
    - `supabase/functions/analyze-ads/index.ts` — +13 entries in `metaToolDefinitions`
    - `supabase/functions/analyze-ads/system-prompt.ts` — expanded "When to Use Tools" + new "Decision Heuristics" in buildMetaSystemPrompt
    - `src/hooks/use-chat-stream.ts` — +13 entries each in TOOL_RISK_LEVEL and TOOL_DESCRIPTION + +13 dispatch branches in approveTool Meta path
    - `src/components/dashboard/ToolApprovalDialog.tsx` — +13 entries in toolDescriptions + App Review banner + platform-agnostic copy
    ```

    Substitute real values for `{today's date YYYY-MM-DD}` and the static-check result placeholders based on Task 1 output.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-VERIFICATION.md';const s=fs.readFileSync(p,'utf8');const need=['Phase 02 Verification','Static Checks','Tool Status','createCampaign','createCustomAudience','createLookalikeAudience','Meta App Review','ads_management_standard_access','custom_audiences','Pre-launch Checklist','npx supabase functions deploy meta-mutate','npx supabase functions deploy analyze-ads','batchPauseEnable','createSplitTest','Files Modified'];const miss=need.filter(t=>!s.includes(t));if(miss.length){console.error('MISSING',miss);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-VERIFICATION.md` exists
    - Contains a "Static Checks" table with 4 rows (tsc, eslint, deno check, tool-name round-trip)
    - Contains a "Tool Status" table with at least 13 rows (one per new tool)
    - Contains "Meta App Review" + "ads_management_standard_access" + "custom_audiences" strings (App Review section)
    - Contains "Pre-launch Checklist" section with deploy commands `npx supabase functions deploy meta-mutate` and `npx supabase functions deploy analyze-ads`
    - Contains "Files Modified" section listing all 5 modified files
    - Contains "Deferred" section listing items from CONTEXT.md
    - All 13 new tool names appear at least once in the file
  </acceptance_criteria>
  <done>VERIFICATION.md documents static-check results, per-tool live-status caveats (especially App Review for audience tools), and the user's pre-launch checklist (including the deploy commands Claude is not permitted to run).</done>
</task>

</tasks>

<verification>
- Static checks (tsc + eslint) pass on the 4 modified frontend/edge files
- 02-VERIFICATION.md created with all required sections
- App Review requirements clearly stated for createCustomAudience + createLookalikeAudience
- Pre-launch deploy commands present (Claude does not run them per M2-05)
- All 13 new tool names round-trip across the 5 modified files
</verification>

<success_criteria>
- User reads 02-VERIFICATION.md and knows exactly which 2 tools need App Review before live usage
- User knows the exact deploy commands to run when ready
- No deploy invoked
- No new lint or type errors introduced by Phase 02 changes
</success_criteria>

<output>
After completion, create `.planning/phases/02-meta-ads-chat-tools-paridade-total-de-manipulacao/02-05-SUMMARY.md`
</output>
