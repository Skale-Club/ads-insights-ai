---
phase: 14-meta-cli
plan: 01
type: execute
wave: 1
depends_on: [13-01]
files_modified:
  - supabase/functions/get-meta-cli-session/index.ts (new)
  - supabase/migrations/20260408200000_meta_cli_sessions.sql (new)
  - src/hooks/useMetaCliSession.ts (new)
  - src/components/settings/MetaClaudeCodeSection.tsx (new)
  - src/pages/settings/index.tsx
  - .claude/meta-ads-cli.md (new)
  - .claude/workflows/meta-ads.md (new)
autonomous: true
---

<objective>
## Goal
Mirror the Google Ads CLI session system for Meta Ads: a `meta_cli_sessions` table, `get-meta-cli-session` edge function, Settings UI to activate/revoke, and full documentation + workflow prompts.

## Output
- Meta CLI session infrastructure (DB + edge function + UI)
- .claude/meta-ads-cli.md
- .claude/workflows/meta-ads.md (5 workflows)
</objective>

<context>
@supabase/functions/get-cli-session/index.ts
@supabase/migrations/20260408000000_cli_sessions.sql
@src/hooks/useCliSession.ts
@src/components/settings/ClaudeCodeSection.tsx
@.claude/google-ads-cli.md
@.claude/workflows/google-ads.md
</context>

<acceptance_criteria>

## AC-1: meta_cli_sessions table
- Same structure as cli_sessions but with `access_token` (Meta) instead of `provider_token`
- 2h expiry, RLS owner policy

## AC-2: get-meta-cli-session edge function
- Same pattern as get-cli-session
- Returns `{ access_token, account_id, account_name, expires_at }`

## AC-3: MetaClaudeCodeSection in Settings
- Activate/Revoke buttons
- Shows `.ads-meta-cli-session.json` config to copy (gitignored)
- Session status + expiry

## AC-4: meta-ads-cli.md documents full curl workflow
- Read session → get-meta-cli-session → meta-reports → meta-mutate
- Approval protocol section (same rules as Google)
- All 4 meta-mutate actions with curl examples

## AC-5: meta-ads.md has 5 Meta workflows
1. Campaign Performance Audit (last 30 days, identify waste + wins)
2. Creative Fatigue Detection (ads > 14 days with declining frequency/CTR)
3. Audience Overlap & Saturation Check
4. Budget Pacing Review (daily/lifetime on track vs. overpacing)
5. Weekly Meta Performance Report (WoW comparison, placement breakdown, creative ranking)
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: DB migration for meta_cli_sessions</name>
  <files>supabase/migrations/20260408200000_meta_cli_sessions.sql</files>
  <action>Mirror 20260408000000_cli_sessions.sql exactly but with table name meta_cli_sessions and access_token column (instead of provider_token).</action>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: get-meta-cli-session edge function</name>
  <files>supabase/functions/get-meta-cli-session/index.ts</files>
  <action>Mirror get-cli-session/index.ts but queries meta_cli_sessions and returns access_token + account_id.</action>
  <done>AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: useMetaCliSession hook + MetaClaudeCodeSection</name>
  <files>src/hooks/useMetaCliSession.ts, src/components/settings/MetaClaudeCodeSection.tsx</files>
  <action>Mirror useCliSession.ts and ClaudeCodeSection.tsx for Meta. Config file: .ads-meta-cli-session.json. Add to Settings page (after MetaAdsSection).</action>
  <done>AC-3 satisfied</done>
</task>

<task type="auto">
  <name>Task 4: Write meta-ads-cli.md</name>
  <files>.claude/meta-ads-cli.md</files>
  <action>
    Full CLI guide following exact structure of .claude/google-ads-cli.md.
    Sections:
    - Prerequisites (company registered, Meta connected, CLI session activated)
    - How to use: read .ads-meta-cli-session.json → call get-meta-cli-session → call meta-reports → call meta-mutate
    - Curl examples for all 6 report types and all 4 mutation actions
    - Human Approval Protocol (same rules as Google, same preview format)
    - Error handling (401=expired token, 190=invalid token, 100=invalid params)
  </action>
  <done>AC-4 satisfied</done>
</task>

<task type="auto">
  <name>Task 5: Write meta-ads workflow prompts</name>
  <files>.claude/workflows/meta-ads.md</files>
  <action>
    5 workflow prompts following same format as .claude/workflows/google-ads.md.

    Each workflow: Purpose, step-by-step data fetching, analysis rules, output format, explicit approval gate before mutations.

    Workflows:
    1. **Campaign Performance Audit** — fetch campaigns + adsets (last 30 days), flag high-spend/low-ROAS, low-frequency (< 1.5 = underspending), identify top/bottom performers
    2. **Creative Fatigue Detection** — fetch ads (last 14 days), flag ads with frequency > 3 AND declining CTR week-over-week; recommend pause + fresh creative
    3. **Audience Overlap Check** — fetch adsets targeting data, identify overlapping interest/custom audiences across campaigns; recommend consolidation
    4. **Budget Pacing Review** — fetch campaign + adset budgets vs. actual spend, calculate pacing rate; flag over/underpacing; recommend adjustments
    5. **Weekly Performance Report** — WoW comparison (spend, reach, ROAS, conversions), top/bottom campaigns, creative ranking by ROAS, placement performance snapshot; executive summary under 400 words
  </action>
  <done>AC-5 satisfied</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/14-meta-cli/14-SUMMARY.md`
Then update .paul/STATE.md and .paul/ROADMAP.md to mark v1.3 complete.
</output>
