---
phase: 12-meta-dashboard
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/pages/dashboard/meta/OverviewPage.tsx (new)
  - src/pages/dashboard/meta/CampaignsPage.tsx (new)
  - src/pages/dashboard/meta/AdSetsPage.tsx (new)
  - src/pages/dashboard/meta/AdsPage.tsx (new)
autonomous: true
---

<objective>
## Goal
Build all 4 Meta dashboard panel pages: Overview (KPIs + daily chart + placement breakdown), Campaigns, Ad Sets, and Ads.

## Output
- 4 new page components in src/pages/dashboard/meta/
</objective>

<context>
@src/pages/dashboard/OverviewPage.tsx (or equivalent Google overview)
@src/hooks/useMetaReport.ts
@.paul/ROADMAP.md (Phase 12 scope section)
</context>

<acceptance_criteria>

## MetaOverviewPage
- KPI cards: Spend, Reach, Impressions, CPM, CTR, CPC, ROAS, Conversions
- Daily spend + impressions line chart (recharts)
- Placement breakdown table (Feed, Stories, Reels, Audience Network, etc.)
- Loading skeletons while data fetches
- Error state if no Meta account connected

## MetaCampaignsPage
- Table: Name, Status badge (ACTIVE/PAUSED/ARCHIVED), Objective, Budget type + amount, Spend, Results, Cost/Result, ROAS
- Pause/Enable action buttons (will call meta-mutate in Phase 13 — stub with disabled state for now)
- Column sorting

## MetaAdSetsPage
- Table: Name, Campaign, Status, Targeting summary, Daily Budget, Spend, Impressions, CTR, ROAS
- Column sorting

## MetaAdsPage
- Table with creative preview (thumbnail 40px if image_url available, else placeholder), Headline, Body (truncated), Status, Spend, Impressions, CTR, CPC, ROAS
- Column sorting
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1-4: Build all 4 Meta page components</name>
  <files>
    src/pages/dashboard/meta/OverviewPage.tsx,
    src/pages/dashboard/meta/CampaignsPage.tsx,
    src/pages/dashboard/meta/AdSetsPage.tsx,
    src/pages/dashboard/meta/AdsPage.tsx
  </files>
  <action>
    Build each page using useMetaReport hook. Mirror the visual style of existing Google Ads pages (same shadcn/ui components, same Tailwind patterns, same loading/error states). Use the KPI card, data table, and chart components already in the codebase where possible.

    Key Meta-specific visual choices:
    - Status badges: ACTIVE=green, PAUSED=yellow, ARCHIVED=gray (same as Google ENABLED/PAUSED pattern)
    - ROAS displayed as "3.2x" format
    - CPM displayed as "$X.XX per 1k impressions"
    - Budget: show "Daily $X" or "Lifetime $X" label
    - Placement names: capitalize and shorten ("facebook_feed" → "FB Feed", "instagram_story" → "IG Stories")
  </action>
  <done>All ACs satisfied</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/12-meta-dashboard/12-SUMMARY.md`
</output>
