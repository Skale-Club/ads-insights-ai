---
phase: 12-meta-dashboard
plan: 01
type: execute
wave: 1
depends_on: [10-03, 11-01]
files_modified:
  - src/hooks/useMetaReport.ts (new)
  - src/components/layout/Sidebar.tsx
  - src/App.tsx
autonomous: true
---

<objective>
## Goal
Add the `useMetaReport` data hook and update routing + Sidebar to be platform-aware. This is the infrastructure layer for Meta dashboard panels before any panel pages exist.

## Output
- src/hooks/useMetaReport.ts
- Sidebar shows Meta nav items when platform=meta
- App.tsx has Meta routes registered
</objective>

<context>
@src/hooks/useGoogleAdsReport.ts
@src/App.tsx
@src/components/layout/Sidebar.tsx (or DashboardLayout.tsx)
</context>

<acceptance_criteria>
## AC-1: useMetaReport hook
- Accepts `(reportType, options)` like useGoogleAdsReport
- Calls `meta-reports` edge function with accessToken + accountId from DashboardContext
- localStorage cache with 5-min TTL (same key pattern: `adsinsight:meta:cache:{type}:{accountId}:{start}:{end}`)
- Disabled when no selectedMetaAccount

## AC-2: Platform-aware Sidebar
- When platform=google: shows existing Google nav (unchanged)
- When platform=meta: shows Meta nav: Overview, Campaigns, Ad Sets, Ads

## AC-3: Meta routes in App.tsx
- /dashboard/meta/overview
- /dashboard/meta/campaigns
- /dashboard/meta/adsets
- /dashboard/meta/ads
- All wrapped in DashboardLayout + ProtectedRoute
- /dashboard redirect goes to /dashboard/overview (Google) or /dashboard/meta/overview (Meta) based on platform
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create useMetaReport hook</name>
  <files>src/hooks/useMetaReport.ts</files>
  <action>Mirror useGoogleAdsReport.ts exactly but for Meta. Use meta-reports edge function. Cache key prefix: adsinsight:meta:cache. Read accessToken from Supabase meta_connections table directly (not from session storage — Meta token lives in DB).</action>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Platform-aware Sidebar</name>
  <files>src/components/layout/Sidebar.tsx (or equivalent)</files>
  <action>Read the file first. Add conditional rendering based on `platform` from useDashboard(). When platform=meta, render Meta nav items. When platform=google, render existing nav (unchanged).</action>
  <done>AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Add Meta routes to App.tsx</name>
  <files>src/App.tsx</files>
  <action>Read App.tsx first. Add 4 Meta routes inside the dashboard route group. Import placeholder components for now (will be replaced in 12-02 and 12-03). Add platform-aware redirect for /dashboard index route.</action>
  <done>AC-3 satisfied</done>
</task>

</tasks>

<output>
No summary — feeds into 12-02.
</output>
